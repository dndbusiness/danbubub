# אפיון מלא — מערכת הכספים הפנימית של הר-אל (SPEC v1)

> מסמך זה נכתב כדי להימסר ל-Claude Code כמסמך דרישות מחייב. הוא מגדיר *מה* המערכת עושה ואיזה כללים עסקיים היא חייבת לאכוף. החלטות טכניות מסומנות במפורש כהמלצה או כפתוחות להכרעה.
>
> **מסמך נלווה מחייב:** `HAREL_FINANCE_SPEC_ADDENDUM.md` — ביקורת כיסוי, אינטגרציות Google (Gmail/Calendar/Drive/Sheets), סיכום יומי, דוח שבועי, צ'קליסט ביצוע וגביה, מצב החברה, מנוע התראות וטבלת כל המשימות המתוזמנות. מסכים 20 (גביה) ו-21 (מצב החברה) מוגדרים שם.
>
> כלל-על: **כל מספר במערכת נגזר משורת נתונים אחת.** אין מסך שמחזיק מספר משלו. הרווח, התזרים, כרטיס ניסים, המע"מ והתחזית — כולם שאילתות על אותן טבלאות.

---

## 0. תמצית מנהלים

מערכת ווב פנימית (5 משתמשים לכל היותר) שמחליפה את הקובץ הרב-גיליוני של הר-אל פתרונות מימון ואת הגיליון הפיננסי של הר-אל השקעות. חשבון בנק אחד, שתי פעילויות, שני מודלי שותפות שונים:

| | הר-אל השקעות נדל"ן | הר-אל פתרונות מימון |
|---|---|---|
| שותפים | דן / אביב / יוני — 33.3% כל אחד | הר-אל (50%) / ניסים (50%) |
| מה מתחלק | רווח אחרי הוצאות; משכורות שוות | רווח אחרי הוצאות קבועות מוכרות + הוצאות ישירות |
| מנגנון תשלום | יוני+נדיה בתלוש (החברה נושאת עלות מעביד); דן ואביב בחשבונית + מע"מ | ניסים בחשבונית; מקדמות חודשיות (מזומן+אשראי, 18–20 לחודש) לשימוש אישי, מקוזזות מחלקו |
| התחשבנות | חו"ז בעלים קטן; חוב ליוני (משקיע) ~200,000 ₪ שיורד | סגירה ב-30 לחודש, העברות עד ה-10 |
| מיוחד | נכיון מזומן מדי פעם | "הכנסות פרייבט" מקרנות — לא של החברה, מתחלק דן/ניסים אישית |

המערכת נותנת בכל רגע: רווח והפסד, מצב התחשבנות עם ניסים, תזרים 13 שבועות, תחזית רבעונית, מצב מע"מ, פערי חשבוניות מול הבנק, ויחסי המרה של לידים.

---

## 1. עקרונות מחייבים (Non-negotiables)

1. **מקור אמת אחד.** טבלת `transactions` היא היחידה שמתעדת כסף שזז. כל השאר נגזר.
2. **הפרדת פעילויות בתוך חשבון אחד.** לכל תנועה, עסקה, הוצאה ומקדמה יש `division` = `realestate` | `finance` | `shared`. `shared` חייב מפתח חלוקה (ברירת מחדל מהגדרות; למשל הדס = 80% מימון / 20% נדל"ן).
3. **הוצאה ≠ מימון ≠ העברה.** `nature` = `expense` | `financing` | `transfer` | `income` | `advance` | `draw` | `vat` | `tax`. רק `expense` ו-`income` נכנסים לרווח והפסד.
4. **מע"מ מפורש בכל שורה.** ברירת מחדל: סכום **ללא מע"מ**. שדה `vat_mode` = `excl` (ברירת מחדל) | `incl` | `exempt`. המערכת שומרת תמיד גם `amount_net` וגם `amount_gross` ו-`vat_amount`. במסך ההזנה יש מתג "הסכום כולל מע"מ" — ברירת מחדל כבוי.
5. **ודאות מפורשת.** `certainty` = `actual` | `committed` | `expected`. רווח והפסד מציג `actual` בלבד. תזרים מציג שני קווים: `actual+committed`, ו-`+expected×הסתברות`.
6. **תקופה נעולה לא משתנה.** אחרי סגירת חודש (כרטיס ניסים, סגירת נדל"ן) התקופה ננעלת. תיקון = תנועת תיקון בתקופה הפתוחה, לעולם לא עריכה של העבר.
7. **יומן שינויים על הכל.** כל יצירה/עריכה/מחיקה: מי, מתי, ערך קודם, ערך חדש.
8. **קריאה בלבד מול העולם.** המערכת לא מבצעת פעולות בבנק/אשראי/חשבונית ירוקה. רק מייבאת קבצים ומשווה.
9. **ארבעה אזורים מוגנים בסיסמה נפרדת + הרשאת תפקיד:** הכנסות פרייבט, משיכות שותפים נדל"ן, כרטיס ניסים, מצב חשבון בנק (עוגן).
10. **ייצוא תמיד זמין.** כל טבלה וכל דוח → XLSX / Google Sheets / PDF / הדפסה.

---

## 2. מודל הנתונים

### 2.1 טבלאות ליבה

#### `entities`
| שדה | סוג | הערות |
|---|---|---|
| id | pk | |
| name | text | א.ד.י הראל השקעות (ח.פ 516857083), D&D (ע.מ), עדן הובלות ובנייה |
| type | enum | `company` / `sole_proprietor` |
| vat_id | text | |

#### `accounts`
| שדה | סוג | הערות |
|---|---|---|
| id | pk | |
| entity_id | fk | |
| type | enum | `bank` / `credit_card` / `cash` / `loan` |
| name | text | "עו"ש הפועלים", "ויזה כאל 1234", "קופה קטנה" |
| billing_day | int | לכרטיס אשראי (2 / 10 / 15) |
| credit_limit | number | מסגרת |
| default_division | enum | `finance` לרוב הכרטיסים (לפי הנחיה קיימת) |
| active | bool | |

#### `balances` (עוגן יומי)
| שדה | סוג |
|---|---|
| date | date |
| account_id | fk |
| balance | number |
| available_credit | number |
| source | enum: `manual` / `statement_import` |
| entered_by | fk users |

#### `transactions` — הטבלה המרכזית
| שדה | סוג | חובה | הערות |
|---|---|---|---|
| id | pk | | |
| date_cash | date | ✓ | מתי הכסף זז בפועל |
| date_doc | date | | תאריך חשבונית/מסמך |
| account_id | fk | ✓ | |
| amount_net | number | ✓ | חתום: חיובי נכנס, שלילי יוצא |
| vat_mode | enum | ✓ | `excl` / `incl` / `exempt` |
| vat_rate | number | ✓ | ברירת מחדל 18% (מהגדרות) |
| vat_amount | number | מחושב | |
| amount_gross | number | מחושב | |
| nature | enum | ✓ | `income` / `expense` / `financing` / `transfer` / `advance` / `draw` / `vat` / `tax` |
| division | enum | ✓ | `realestate` / `finance` / `shared` / `private` |
| division_split | json | | רק ל-`shared`: `{finance:0.8, realestate:0.2}` |
| category_id | fk | ✓ להוצאות | |
| tx_class | enum | ✓ | `business` / `vehicle` / `private` (סיווג סוג העסקה שביקשת) |
| deductible | bool | ✓ להוצאות | "מוכרת לפעילות" — קובע אם נכנסת לרווח לחלוקה |
| fixed_expense_id | fk | | אם זו התממשות של הוצאה קבועה |
| deal_id | fk | | אם זו הוצאה ישירה / תקבול על תיק |
| counterparty | text | | ספק / לקוח / ניסים / יוני |
| description | text | | |
| certainty | enum | ✓ | `actual` תמיד לטבלה זו; צפי יושב ב-`scheduled`/`deals` |
| parent_id | fk self | | לפירוט כרטיס אשראי: הבנות מצביעות על חיוב-האב |
| invoice_status | enum | ✓ | `has_invoice` / `missing` / `no_invoice_needed` / `unknown` |
| invoice_id | fk | | קישור למסמך |
| review_status | enum | | `ok` / `ask_nissim` / `ask_aviv` / `ask_yoni` / `unknown_expense` |
| review_note | text | | |
| period_id | fk | | תקופת ההתחשבנות שאליה משויכת (נעילה) |
| source | enum | | `manual` / `card_import` / `bank_import` / `greeninvoice_import` / `system` |
| source_ref | text | | מזהה שורה במקור לצורך dedup |
| created_by / updated_at | | | |

**אילוצים:**
- אם `parent_id` מלא → השורה לא נספרת בתזרים (רק לסיווג). סכום הבנות = סכום האב (בדיקה אוטומטית, אזהרה בסטייה).
- `nature=advance` חייב `counterparty=ניסים` ו-`division=finance`.
- `nature=draw` חייב שותף מ-`partners`.
- `division=private` לא נכנס לשום דוח עסקי, רק למסך "פרטי/עסקי" של הכרטיס.

#### `categories`
עץ קטגוריות עם `kind` = `fixed` / `direct` / `variable` / `owner`. ברירות מחדל ראשוניות: שיווק, לידים (אבינועם / אלכס), שכר, ביטוח לאומי מעסיק, משרד, תוכנה ומערכות, רו"ח, הלוואה, רכב יורם, כיבוד, מנהל קמפיינים, נסח טאבו, שמאות, עו"ד, דוח BDI/אשראי, הנה"ח לקוח.

#### `fixed_expenses` (לוח חיובים קבועים — `scheduled`)
| שדה | הערות |
|---|---|
| name, category_id, division, division_split | |
| amount_net, vat_mode | |
| frequency | `monthly` / `quarterly` / `yearly` / `once` |
| day_of_month | |
| account_id | מאיזה חשבון יוצא |
| variable | bool — סכום משתנה (אשראי) לעומת קבוע (שכירות) |
| approved_by_nissim | bool — "הוצאה קבועה מאושרת" ל-50/50 |
| start_date / end_date | |
| active | |

מהטבלה הזו נולדות שורות `expected` בתזרים קדימה (13 שבועות) ומולן מתבצעת ההתאמה כשמגיע החיוב בפועל.

#### `deals` (תיקים / עסקאות)
| שדה | הערות |
|---|---|
| id, client_name, client_phone | |
| division | `finance` / `realestate` |
| product | מימון: `mortgage_declined` / `business_credit` / `vehicle_lien` / `other`; נדל"ן: `presale` / `secondhand` |
| stage | שלב תפעולי (רשימה סגורה — סעיף 2.3) |
| collection_status | סטטוס גביה (רשימה סגורה) |
| fee_agreed_net | שכ"ט שנסגר (ללא מע"מ) |
| fee_mode | `fixed` / `pct_of_credit` / `pct_of_property` |
| fee_pct, base_amount | ל-% : אחוז וסכום הבסיס (אשראי שאושר / שווי דירה) |
| advance_at_signing_net | מקדמה בחתימה (0 בשעבוד רכב) |
| developer_commission_net | נדל"ן בלבד, net-30, אחרי ניכוי מס במקור |
| opening_fee_net | נדל"ן |
| expected_close_date | |
| probability_override | אופציונלי — דורס הסתברות לפי שלב |
| lead_source_id | fk — מאיזה ערוץ הגיע |
| month_attributed | **חודש ההתחשבנות** שאליו העסקה משויכת (ראה כלל 3.4) |
| owner_user_id | מי מטפל |
| status | `open` / `won` / `lost` / `cancelled` |
| cancelled_fee_net | 2,000 ₪ במקרה ביטול אחרי יום עסקים |
| last_activity_at | ל"רקב" אוטומטי |
| notes | |

**נגזרות (לא שדות):** `collected_net` = Σ תנועות `income` עם `deal_id` ו-`certainty=actual`; `open_balance_net` = `fee_agreed_net − collected_net`; `direct_costs_net` = Σ תנועות `expense` עם `deal_id`; `net_contribution` = `collected − direct_costs`.

#### `deal_payments_plan` (לוח תקבולים צפוי לתיק)
| שדה | הערות |
|---|---|
| deal_id | |
| label | "מקדמה בחתימה" / "success fee" / "עמלת יזם" / "יתרה" |
| amount_net | |
| expected_date | |
| certainty | `committed` / `expected` |
| probability | 0–1 |
| matched_tx_id | fk transactions — כשנכנס בפועל |

זה מה שמאפשר "שולם חלקית ויש יתרה — פוטנציאלית או בטוחה לכאורה": כל שורה נושאת את הודאות שלה בנפרד.

#### `advances` (מקדמות לניסים)
| שדה | הערות |
|---|---|
| date, amount_gross | מקדמה היא כסף שיצא — נרשמת בברוטו, ראה שאלה פתוחה #4 |
| method | `cash` / `credit_card` / `transfer` |
| tx_id | fk — התנועה בפועל |
| period_id | חודש הקיזוז |
| note | |

#### `partners` + `partner_draws`
partners: דן, אביב, יוני(+נדיה), ניסים; `share_pct` לפי division; `pay_method` = `invoice` / `payslip`.
partner_draws: תאריך, שותף, סכום, סוג (`salary` / `management_fee` / `dividend` / `loan_repayment` / `owner_loan`), tx_id. **לחוב יוני 200,000 ₪:** רשומת `owner_loans` עם יתרת פתיחה, וכל החזר = `loan_repayment` שמקטין אותה.

#### `periods` (תקופות התחשבנות)
| שדה | הערות |
|---|---|
| division, year, month | |
| status | `open` / `closed` |
| closed_at, closed_by | |
| snapshot_json | תצלום כל המספרים ברגע הסגירה — לא מחושב מחדש אחר כך |
| settlement_transfer_tx_id | ההעברה בפועל שסגרה את החודש |

#### `invoices` (מסמכים — יוצאים ונכנסים)
| שדה | הערות |
|---|---|
| direction | `issued` (שלנו, מחשבונית ירוקה) / `received` (ספק) |
| doc_type | חשבונית מס / קבלה / חשבונית-קבלה / זיכוי |
| doc_number, date, counterparty | |
| amount_net, vat_amount, amount_gross | |
| entity_id | |
| matched_tx_id | |
| source | `greeninvoice_import` / `manual` / `upload` |
| file_url | סריקה |

#### `leads` + `lead_sources` + `lead_costs`
- `lead_sources`: אבינועם, אלכס, פייסבוק, גוגל, הפניה, רכב-לנדינג, וכו'. `cost_model` = `per_lead` / `monthly` / `pct`.
- `leads`: תאריך, מקור, product, שלב (`received` → `contacted` → `meeting` → `proposal` → `signed` → `closed_won`/`lost`), deal_id אם הומר. שדה `week` מחושב.
- `lead_costs`: תאריך, מקור, סכום, tx_id. (150 ₪/ליד אבינועם; 200 ₪/ליד אלכס בחבילות ~2.5 חודשים — נפרס לפי שבועות.)

#### `private_income` (אזור מוגן)
| שדה | הערות |
|---|---|
| fund_name | הקרן |
| deal_ref | |
| deal_amount | סכום העסקה בקרן |
| threshold_rule | טקסט/json — למשל "מעל 3M חודשי → 1%" |
| pct | |
| amount_net, vat_amount | |
| split_dan, split_nissim | ברירת מחדל 50/50 — נפרד לחלוטין מהחברה |
| received_date, received_to | חשבון פרטי |
| status | `expected` / `received` |

**כלל:** טבלה זו לא מצטרפת לשום שאילתה של `transactions`. אם הכסף נחת בטעות בחשבון החברה — נרשם כ-`transfer` החוצה ומסומן.

#### `employees` + `employment_terms` + `payroll_months` (מודול שכר)

**`employees`:** שם, ת.ז./ע.מ., `pay_type` = `payslip` / `invoice` / `freelancer`, `division_split` (הדס: 80/20), תאריך תחילה/סיום, פעיל.

**`employment_terms`** — ההסכם, עם תוקף (שינוי הסכם = שורה חדשה, לא עריכה):
| שדה | הערות |
|---|---|
| employee_id, valid_from, valid_to | |
| base_mode | `hourly` / `monthly` / `none` (פרילנסר על עמלות בלבד) |
| hourly_rate / monthly_base | |
| expected_hours | לתקן משרה — אופציונלי |
| components | json — רשימת רכיבי בונוס, כל אחד: `{name, type, rate, unit, cap, floor, vat_mode}` |
| employer_cost_pct | אחוז עלות מעביד משוער (ביטוח לאומי + פנסיה + פיצויים; ברירת מחדל מהגדרות, ~22–25%) |
| notes | |

**סוגי רכיב (`type`):**
- `per_unit` — × כמות: בונוס פגישות (₪/פגישה), בונוס מכירות (₪/סגירה), ₪/ליד מטופל
- `pct_of_sales` — % מ-Σ שכ"ט שנסגר/נגבה בתיקים שמשויכים לעובד (`deals.owner_user_id` או `closer_id`)
- `fixed` — בונוס קבוע חודשי
- `tiered` — מדרגות: `[{from:0,to:5,rate:200},{from:6,to:null,rate:350}]`
- `conditional_fixed` — בונוס קבוע בתנאי סף (למשל ≥10 פגישות בחודש)

**`payroll_months`** — שורה לעובד לחודש:
| שדה | הערות |
|---|---|
| employee_id, period (YYYY-MM) | |
| hours_worked, meetings_count, sales_count | קלט ידני / ייבוא מגיליון שעות / **אוטומטי מהמערכת** (מכירות = תיקים שנחתמו עם `closer_id`; פגישות — מ-WISE אם מיוצא, אחרת ידני) |
| sales_amount_net | Σ שכ"ט לרכיבי % — נגזר |
| manual_adjustments | json: `[{label, amount, note}]` — מקדמה, ניכוי, החזר הוצאות, ימי חופש |
| computed | json snapshot: כל רכיב + סכום + הסבר החישוב |
| gross_total | ברוטו (או סכום החשבונית לפרילנסר, לפני מע"מ) |
| employer_cost_est | ברוטו × (1 + employer_cost_pct) — לתלוש בלבד |
| status | `draft` / `approved` / `sent_to_accountant` / `paid` |
| approved_by, approved_at | |
| paid_tx_id | התנועה בפועל בבנק (נטו) — ההתאמה |
| report_file_url | ה-PDF שנשלח לרו"ח |

**כלל:** `payroll_months.status=approved` → נוצרת אוטומטית שורת `expected` בתזרים (עלות מעביד, ביום התשלום מההגדרות, למשל ה-9) ושורת `expense` ב-P&L לפי `division_split`. משכורת של יוני+נדיה עוברת דרך אותו מודול אבל מסומנת `nature=draw` (משיכת שותף, סעיף 3.5) ולא `expense`.

#### `links`, `tasks`, `rules`, `audit_log`, `users`, `questions`
- `links`: כותרת, url, קטגוריה (בנקים / קרנות / חשבונית ירוקה / CRM / שיווק), אייקון, סדר.
- `tasks`: מודול תפעול — כותרת, אחראי, תאריך יעד, עדיפות, סטטוס, קשור-ל (deal_id / tx_id / period_id), תת-משימות, הערות. **הפעולה החשובה:** כל פער שהמערכת מוצאת (חשבונית חסרה, תנועה לא מזוהה, עסקה שנרקבה) יוצר משימה אוטומטית.
- `rules`: `pattern` (טקסט/regex על תיאור התנועה), `account_id` אופציונלי → `category_id`, `division`, `nature`, `tx_class`, `invoice_status`. נלמד מכל סיווג ידני (שאלה "להפוך לכלל?").
- `questions`: שורות "לשאול את ניסים/אביב" — נגזרת של `review_status`, עם מסך אחד שמאפשר לשותף לענות ולסגור.
- `users`: role = `admin` (דן) / `partner_finance` (ניסים) / `partner_realestate` (אביב, יוני) / `backoffice` (הדס) / `accountant` (קריאה).

### 2.2 שלוש ישויות משפטיות בחשבון אחד
כרגע כל הפעילות באותו חשבון בנק. שדה `entity_id` נמצא ב-`accounts`, ו-`division` ב-`transactions`. כשתבצע הפרדת חשבונות — נוסיף חשבון חדש ומשנים `default_division`. שום דוח לא צריך להשתנות.

### 2.3 רשימות סגורות (ניתנות לעריכה בהגדרות)

**שלבי תיק מימון:** לקוח פוטנציאלי → לקוח חתם / איסוף מסמכים → הוגש לגוף מממן → אישור עקרוני → שמאות → חתימה אצל עו"ד → ביצוע → הושלמה העסקה.

**שלבי תיק נדל"ן:** ליד → פגישה → הצעה → חתימת חוזה → תשלום עמלה → עמלת יזם התקבלה → נסגר.

**סטטוס גביה:** לא נגבה / שולם מקדמה / שולם חלקית / שולם במלואו / גביה משפטית / מבוטל.

**הסתברות לפי שלב (ברירת מחדל, ניתנת לכיול):** פוטנציאלי 0% · חתם 30% · הוגש 50% · אישור עקרוני 70% · שמאות/עו"ד 85% · ביצוע 95% · הושלם 100% (→ committed).
**רקב:** אין פעילות 30 יום → ההסתברות נחתכת לחצי. 60 יום → 0 ומשימה "לעדכן/לסגור".

---

## 3. כללים עסקיים מקודדים

### 3.1 מע"מ
- `vat_rate` בהגדרות (18%). היסטוריה של שיעורים לפי תאריך.
- **חבות מע"מ בכל רגע:** `Σ vat_amount` על הכנסות `actual` בתקופה − `Σ vat_amount` על הוצאות `actual` **שיש להן חשבונית** (`invoice_status=has_invoice`) בתקופה. מוצג בנפרד: "מע"מ תשומות שחסר לו חשבונית" — כמה כסף אתה מפסיד אם לא תשיג אותן.
- דו-חודשי / חודשי — לפי הגדרה. תאריך דיווח (15 לחודש) נכנס אוטומטית ללוח החיובים כ-`expected` (`nature=vat`).
- הכנסות פרייבט מקרנות: "+ מע"מ" — נרשם ב-`private_income` בלבד, **לא** בחישוב מע"מ החברה.

### 3.2 רווח והפסד (P&L) — שתי הגדרות, בכוונה
| | רווח תפעולי (מה נשאר בקופה) | רווח לחלוקה (הסכם השותפות) |
|---|---|---|
| הכנסות | כל `income actual` בתקופה | `income actual` בתקופה עם `deal_id`, לפי `month_attributed` |
| הוצאות קבועות | הכל | רק `deductible=true` **ו-** `approved_by_nissim=true` (במימון) |
| הוצאות ישירות | נכללות | נכללות (Σ לפי `deal_id`, לפי חודש התיק) |
| מקדמות / משיכות | לא נכללות (זה `advance`/`draw`) | לא נכללות |
| מימון | לא | לא |

מסך אחד, מתג בין שתי ההגדרות, כותרת ברורה איזו מוצגת. PDF/הדפסה מכל מצב.

### 3.3 כרטיס ניסים — סגירת חודש (`division=finance`)
```
1. הכנסות_נגבו        = Σ income actual, division=finance, month_attributed=M
2. הוצאות_קבועות_מוכרות = Σ expense actual, division∈{finance, shared×split}, deductible=1, approved=1, period=M
3. הוצאות_ישירות       = Σ expense actual with deal_id, deal.month_attributed=M
4. רווח_לחלוקה         = 1 − 2 − 3
5. חלק_ניסים           = רווח_לחלוקה × 50%        (הפסד → חלק שלילי; ראה שאלה פתוחה #5)
   חלק_הר-אל           = היתרה (זה "50% הר-אל השקעות — כל השותפים")
6. מקדמות_החודש        = Σ advances, period=M
7. יתרת_חוב_סוף_חודש   = יתרה_פתיחה + מקדמות_החודש − חלק_ניסים
   חיובי = ניסים חייב לחברה · שלילי = החברה חייבת לניסים
8. העברה_נדרשת_עד_10   = max(0, −יתרת_חוב)  (אם החברה חייבת — מעבירים; אם ניסים חייב — מתגלגל)
```
**הוצאות קבועות מאושרות יורדות מהחשבון 50/50:** אין תנועה מיוחדת. הן יוצאות מחשבון החברה פעם אחת (`expense`), ומקוזזות מהרווח *לפני* החלוקה — כלומר ניסים נושא ב-50% מהן דרך החלוקה. הדבר היחיד שיורד רק מחלקו הוא `advance`.

**"מנקים שולחן":** יתרת פתיחה לספטמבר/אוקטובר 2026 מוזנת ידנית פעם אחת כ-`opening_balance` בטבלת `periods` (מהחישוב הקיים: ניסים חייב ~36,517 ₪ נכון לספטמבר — לאמת מולו). מרגע זה הכל מחושב.

**תהליך הסגירה במסך:**
1. תצוגה מקדימה של 8 השורות עם drill-down לכל מספר.
2. רשימת חריגים שחוסמים סגירה: תנועות `unknown_expense`, תיקים בלי `month_attributed` שיש בהם כסף, מקדמות בלי חודש, בנות אשראי שלא מסתכמות לאב.
3. אישור → `periods.status=closed` + snapshot + PDF "דוח התחשבנות M/YYYY" + משימה "להעביר X ₪ עד ה-10" + שדה לקישור ההעברה בפועל.

### 3.4 שיוך עסקה לחודש (`month_attributed`)
ברירת מחדל: החודש שבו נכנס **התקבול הראשון** (`date_cash` הראשון). ניתן לדריסה ידנית. תקבול שני של תיק (יתרה על בסיס הצלחה) — נספר בחודש שבו הוא נכנס, **לא** בחודש המקורי של התיק. (זה מתקן את ליקוי #3 מהקובץ הקיים: תקבול חלקי לא נספר כמלוא השכ"ט.)

### 3.5 הר-אל השקעות — 33/33/33
```
רווח_לחלוקה_נדל"ן(M) = income actual (realestate) − expense actual (realestate + shared×split), deductible=1
יעד_משיכה_שוויוני(YTD) = Σ רווח_לחלוקה_YTD / 3
משיכה_בפועל(שותף) = Σ partner_draws (salary+management_fee+dividend), YTD
   • יוני: עלות תלוש **כולל עלות מעביד** (יוני + נדיה יחד) — זה מה שנספר כמשיכה
   • דן/אביב: חשבונית — נספר הסכום **ללא מע"מ**
סטייה(שותף) = משיכה_בפועל − יעד
חו"ז_בעלים(שותף) = Σ owner_loan − Σ loan_repayment
חוב_ליוני = 200,000 (פתיחה) − Σ repayments → מוצג עם קצב ירידה
נכיון_מזומן = nature=income, method=cash, flag cash_discount=true → מוצג בנפרד בדוח (לא מוסתר)
```
מסך "משיכות שותפים" — מוגן בסיסמה, נגיש לאדמין בלבד (ואופציונלית לאביב/יוני לצפייה).

### 3.6 תזרים 13 שבועות
```
יתרה_צפויה(T) = עוגן_אחרון
              + Σ deal_payments_plan committed עד T
              − Σ fixed_expenses (expected) עד T
              − Σ advances צפויות (ברירת מחדל: ממוצע 3 חודשים אחרונים, ביום 18–20)
              − חובות מע"מ/מס צפויים
              [+ Σ deal_payments_plan expected × probability]   ← קו שני
```
שתי בדיקות אוטומטיות: **סגירת יום** (צפי אתמול − עוגן היום = סטייה → תור "מה קרה?") ו**סגירת חודש** (ייבוא דף בנק/אשראי → התאמה שורה-שורה).
שלוש התראות בלבד: יתרה צפויה שלילית (עם תאריך), סטיית יום מעל סף, עוגן לא עודכן 48 שעות.

### 3.7 תחזית רבעונית — נפרדת לכל פעילות, מאוחדת לחשבון

**עיקרון:** שתי הפעילויות מתנהגות אחרת לגמרי, ולכן לכל אחת מנוע תחזית משלה. התוצאה מתאחדת רק בשורה אחת — התזרים בחשבון הבנק המשותף.

#### 3.7.1 הר-אל השקעות נדל"ן
| מרכיב | איך מחושב |
|---|---|
| קצב עסקאות | ממוצע נע 12 שבועות של חוזי דירה שנחתמו (שלב "חתימת חוזה"), לפי חודש |
| ערך עסקה ממוצע | ממוצע נע של (עמלת אחוזים + דמי פתיחה) ו-(עמלת יזם) בנפרד — כי הן בתזמון שונה |
| הכנסה מיידית | עסקאות צפויות × (אחוזים + פתיחה) — בחודש החתימה |
| עמלת יזם | עסקאות צפויות × עמלת יזם ממוצעת × (1 − ניכוי במקור) — **בחודש +1** (net-30) |
| פייפליין קיים | תיקים פתוחים לפי שלב × הסתברות (2.3) × סכומיהם, בתאריך הצפוי |
| הוצאות | קבועות נדל"ן + חלק נדל"ן בהוצאות משותפות (20% הדס וכו') + לידים (150/200 ₪ × צפי לידים) + רכב יורם + עמלות סגירה (יורם, success-based) |
| משיכות | שכר יוני+נדיה (עלות מעביד, קבוע) + דמי ניהול דן/אביב (לפי ממוצע או יעד שוויוני) |
| חוב יוני | החזר חודשי מתוכנן (הגדרות) |
| פלט | לכל אחד מ-3 החודשים: עסקאות צפויות · הכנסה מיידית · עמלות יזם (מהחודש הקודם + החדשות) · הוצאות · רווח לחלוקה · חלק לכל שותף (÷3) · תזרים נטו |

#### 3.7.2 הר-אל פתרונות מימון
| מרכיב | איך מחושב |
|---|---|
| קצב חתימות | ממוצע נע 8 שבועות של הסכמים חתומים, לפי מוצר (משכנתא מסורבים / אשראי עסקי / שעבוד רכב) |
| שכ"ט ממוצע | לפי מוצר (3–5% מהתיק, 250K–2M; רכב 10%) — נמדד מהנתונים |
| זמן חתימה→גביה | התפלגות בפועל לפי מוצר (נקודת פתיחה: 3–8 שבועות; רכב מיידי). התחזית פורסת כל חתימה צפויה לפי ההתפלגות, לא לפי ממוצע אחד |
| מקדמות בחתימה | חתימות צפויות × מקדמה ממוצעת — מיידי (רכב: 0) |
| פייפליין קיים | `deal_payments_plan` committed + expected × הסתברות (מזין את רוב הרבעון הקרוב — 66% מהפייפליין הוא "על בסיס הצלחה") |
| הגשות לבנקים | אחוז אישור לפי גוף מממן (מ-WISE, 4.5) מכייל את ההסתברות |
| הוצאות | קבועות מאושרות מימון + 80% הדס + תוכנות + הוצאות ישירות ממוצעות לתיק × תיקים |
| ניסים | חלק צפוי (50% × רווח לחלוקה) · מקדמות צפויות (ממוצע 3 חודשים) · יתרה מתגלגלת צפויה בסוף הרבעון |
| פלט | לכל חודש: חתימות · נגבה צפוי (מפייפליין / מחתימות חדשות) · הוצאות · רווח לחלוקה · חלק ניסים · חלק הר-אל · יתרת ניסים צפויה |

#### 3.7.3 מאוחד
- שורה אחת לחשבון: תזרים נטו נדל"ן + תזרים נטו מימון − הוצאות משותפות (פעם אחת, לא כפול) − מע"מ − משיכות = יתרה צפויה לסוף כל חודש.
- שלושה תרחישים לכל פעילות בנפרד: **פסימי** = committed בלבד, אפס חתימות חדשות · **בסיס** = קצב נוכחי · **אופטימי** = קצב × 1.2. אפשר לשלב (נדל"ן פסימי + מימון בסיס).
- מסך 13: שני טאבים (נדל"ן / מימון) + טאב "מאוחד". בכל טאב: טבלת 3 חודשים × שורות הפלט, גרף, ובורר תרחיש. כל הנחה (קצב, ממוצע, זמן גביה) מוצגת ליד המספר וניתנת לדריסה ידנית לתרחיש "מה אם".
- **דיוק התחזית:** כל חודש שנסגר, המערכת שומרת מה חזתה 30/60/90 יום קודם מול מה קרה. מוצג כאחוז — כדי שתדע כמה לסמוך.

### 3.8 יחס המרה שבועי
לכל שבוע × ערוץ × מוצר:
```
לידים_התקבלו · נוצר_קשר · פגישות · הצעות · חתימות · סגירות(נגבה)
המרה_לחתימה = חתימות / לידים · המרה_לגביה = סגירות / לידים
עלות_ליד = Σ lead_costs(ערוץ) / לידים
עלות_רכישה_לקוח(CAC) = Σ lead_costs(ערוץ) / חתימות
הכנסה_לליד = Σ income actual (deals from ערוץ) / לידים
ROI ערוץ = הכנסה / עלות
```
תצוגה: טבלה שבועית + היטמאפ ערוץ×מוצר + "מסקנות אוטומטיות" (ערוץ עם CAC מעל ממוצע ×1.5 → דגל; ערוץ עם 0 חתימות ב-4 שבועות → דגל).

### 3.9 תחשיב שכר חודשי
```
לכל עובד, לכל חודש, לפי ההסכם שבתוקף בתאריך 1 לחודש:
  בסיס        = hourly_rate × hours_worked   |   monthly_base   |   0
  לכל רכיב r:
    per_unit           → rate × כמות(r)         (עד cap אם הוגדר)
    pct_of_sales       → rate × sales_amount_net (לפי בסיס: נסגר / נגבה — נבחר בהסכם)
    fixed              → rate
    tiered             → Σ לפי מדרגות על הכמות
    conditional_fixed  → rate אם כמות ≥ סף, אחרת 0
  ברוטו       = בסיס + Σ רכיבים ± manual_adjustments
  עלות_מעביד  = ברוטו × (1 + employer_cost_pct)      [תלוש]
              = ברוטו (+ מע"מ בנפרד)                  [חשבונית / פרילנסר]
  חלוקה       = עלות_מעביד × division_split  →  P&L של כל פעילות
```
- כל מספר ב-`computed` נשמר עם ההסבר ("12 פגישות × 150 ₪ = 1,800 ₪") — זה מה שמופיע בדוח לרו"ח ובמה שהעובד רואה.
- שינוי הסכם באמצע חודש → שני חישובים פרו-רטה לפי ימים, שקופים בדוח.
- אין עריכה של חודש `approved`; תיקון = `manual_adjustment` בחודש הבא עם הפניה.

---

## 4. ייבוא, התאמה ובקרה

### 4.1 ייבוא כרטיס אשראי (Excel/CSV)
- תמיכה בפורמטים של ישראכרט, מקס, כאל (מיפוי עמודות בהגדרות; זיהוי אוטומטי לפי כותרות).
- כל קובץ → תנועת-אב אחת (החיוב בבנק, `date_cash`=יום החיוב) + בנות. אם האב כבר קיים מייבוא בנק — משדכים.
- לכל בת: הרצת `rules` → הצעת סיווג. מסך אישור ב-bulk: קטגוריה, `division`, `tx_class` (עסקי/רכב/פרטי), `invoice_status`, `nature` (הוצאה / מקדמה-ניסים אם הכרטיס משמש אותו אישית / פרטי).
- שורה שאינה מזוהה → `review_status=unknown_expense` → כפתור "לשאול את ניסים / אביב" → משימה.
- dedup לפי `source_ref` (מס' עסקה + תאריך + סכום).

### 4.2 ייבוא דפי בנק (PDF + CSV/XLSX)
- CSV/XLSX: מיפוי עמודות לפי בנק (הפועלים/לאומי/דיסקונט/מזרחי — יוגדר לפי הבנק בפועל, שאלה פתוחה #1).
- PDF: חילוץ טבלאות (pdfplumber / camelot); אם נכשל → OCR; תוצאה תמיד עוברת מסך אימות לפני שמירה.
- **התאמה:** לכל שורת בנק → חיפוש תנועה קיימת (סכום זהה ± 1 ₪, תאריך ± 3 ימים, אותו חשבון). התאמה מלאה / מועמדים / ללא התאמה. ללא התאמה → יצירת תנועה חדשה עם `review_status=unknown_expense`.
- בסוף: יתרת סגירה בדף = יתרה מחושבת? אם לא — הפער מוצג ולא נסגר החודש.

### 4.3 חשבונית ירוקה (morning) — דוח הנה"ח מול הבנק

**פורמט ייצוא הוצאות (מאומת מקובץ אמיתי, 291 שורות):** CSV, UTF-8 עם BOM, 14 עמודות:
`מספר המסמך · תאריך המסמך (dd/mm/yyyy) · חודש דיווח (MM/yyyy) · סוג המסמך · ספק · חשבון הוצאה · תיאור · סכום כולל מע״מ · מע"מ · סכום לא כולל מע״מ · מטבע · סכום הוצאה עסקית (₪ אחרי המרה) · מע״מ הוצאה עסקית · סטטוס`
- `סוג המסמך` ∈ {חשבונית מס, חשבונית מס / קבלה, קבלה, חשבון / אישור תשלום, חשבונית זיכוי} — זיכוי = סכום שלילי.
- `סטטוס` ∈ {הוצאה מדווחת, הוצאה פתוחה}. "פתוחה" = טרם אושרה/דווחה — נכנסת ל-`invoices` עם `reported=false`.
- `מטבע` ∈ {ILS, USD, EUR} — **תמיד לוקחים את "סכום הוצאה עסקית" ו"מע״מ הוצאה עסקית"** (כבר ב-₪), לא את הסכום המקורי.
- `חודש דיווח` ≠ חודש המסמך לפעמים — זה חודש המע"מ. נשמר כ-`vat_period`.
- **מפתח dedup:** (מספר מסמך, ספק, סכום כולל). בקובץ האמיתי יש כפילויות (למשל מס' 3 / De-Identification / 5.9 ₪; מס' 31557 / מאג 5 / 3,800 ₪) — הייבוא מסמן אותן `possible_duplicate` ולא מייבא פעמיים בלי אישור.
- **נרמול ספק:** אותו ספק מופיע בכתיבים שונים ("הנכס בברודצקי 43 בע\"מ" / "בעמ"). טבלת `suppliers` עם aliases; ההתאמה לבנק נעשית לפי ספק מנורמל.

**ממצא קריטי מהקובץ:** כל 291 ההוצאות מסווגות בחשבונית ירוקה תחת קטגוריה אחת — "עלויות אחרות" — ורק 20 מהן עם תיאור. כלומר הסיווג בחשבונית ירוקה **לא קיים**, והמערכת שלנו היא זו שמסווגת. הפלט לרו"ח (סעיף 7) כולל עמודת "מפתח חשבון" לפי רשימת סוגי ההוצאות של חשבונית ירוקה (3011 שכר עבודה, 3012 נלוות לשכר, 3540 הנה"ח, 3560 נסיעות, 3570 שכירות, 3575 ארנונה/מיסים, 3590 חשמל/מים, 3625 כיבודים 80%, 3650 תקשורת, 3680 תוכנה, 1390 עלויות אחרות) — כל `category` שלנו ממופה למפתח כזה בהגדרות. שיפור אופציונלי: לתקן את הסיווג גם בחשבונית ירוקה עצמה.

**ממצא שני — חשבוניות של השותפים לחברה:** "די.אנד.די. עסקים" (העוסק של דן) הוא הספק הנפוץ ביותר בקובץ (58 מסמכים), כולל "דמי ניהול הר-אל כולל ניסים" 27,651 ₪. חשבוניות כאלה **אינן הוצאה** — הן `nature=draw` (משיכת שותף) או `advance`/התחשבנות ניסים. כלל אוטומטי: ספק ∈ {די.אנד.די, החשבונית של אביב, החשבונית של ניסים} → `nature=draw`, `partner_id` לפי ספק, ודורש אישור. שאלה פתוחה #15.

- ייבוא **דוח הכנסות/מסמכים** (אותו מבנה, מסמכים שהוצאנו): כל מסמך → `invoices(direction=issued)`; חשבונית מס ללא קבלה = חוב לקוח פתוח.
- ייבוא חשבוניות ספקים מהייצוא הנ"ל → `invoices(direction=received)`; צילום/PDF ידני כגיבוי.
- חשבונית ירוקה כבר מציעה קליטת הוצאות בוואטסאפ ו"הוצאות לאישור" (41 ממתינות כרגע) — ממשיכים להשתמש בזה כשער הכניסה של המסמכים; המערכת שלנו מייבאת משם, לא מחליפה.
- **בדיקה 1 — הכנסות:** לכל `income actual` בבנק: יש חשבונית מס/קבלה בסכום תואם (ברוטו)? אם לא → "הכנסה ללא חשבונית" (משימה: להוציא חשבונית). ולהפך: חשבונית שהוצאה ואין לה תקבול → "חשבונית פתוחה" (=חייבים לנו).
- **בדיקה 2 — הוצאות:** לכל `expense actual` שמסומנת `invoice_status∈{unknown, missing}`: חיפוש חשבונית ספק. אין → רשימת "חסר לי חשבונית" עם סכום המע"מ המפוספס.
- **בדיקה 3 — שנה אחורה:** אותו תהליך על 12 חודשים, פלט: טבלת פערים לפי חודש: הכנסות בלי מסמך / מסמכים בלי כסף / הוצאות בלי חשבונית / הפרשי סכום.
- כל תנועה עם `invoice_status=no_invoice_needed` (עמלות בנק, ביטוח לאומי, מס) — נדלגת אוטומטית לפי כלל.

### 4.4 מנוע כללים לומד
כל סיווג ידני מציע "להפוך לכלל?". כלל = תבנית טקסט (+ חשבון אופציונלי) → ערכי ברירת מחדל. אחרי חודשיים היעד: 90% מהתנועות מסווגות אוטומטית.

### 4.5 ייבוא מ-WISE (מערכת ייעוץ המשכנתאות — app.wisecard.co.il)
**עיקרון: WISE נשארת מקור האמת התפעולי ללידים, לקוחות והגשות לבנקים. המערכת שלנו היא מקור האמת לכסף.** אין הזנה כפולה — מייבאים ומחברים.

| מסך ב-WISE | ייצוא | יעד במערכת | מפתח שידוך |
|---|---|---|---|
| רשימת מתעניינים | "ייצוא טבלה לאקסל" | `leads` | טלפון נייד (+ אימייל) |
| לקוחות | ייצוא | `deals` (יצירה/עדכון; `division=finance`) | ת.ז. / טלפון |
| סטטוס בקשות (הגשות לבנקים) | ייצוא | `deal_submissions` (טבלה חדשה: deal_id, בנק, סניף, תאריך הגשה, סטטוס, תאריך אישור) | שם לקוח + בנק + תאריך |

**מיפוי שדות מתעניין:** שם, טלפון, אימייל, מקור הפניה (`טופס – אתר אינטרנט` / `הוכנס ידנית`), תאריך הקמה, סטטוס מעקב (`לא רלוונטי` / `אין מענה` / `נסגר וחתם` / `לחשוב על זה` / `קמפיינים ישנים` / `ליד חדש` / `נשלחו דוחות משכנתא` / `לקוח נסגר ועבר ללקוחות` / `חדשים לא נענו`), פעילות אחרונה (טקסט חופשי — נשמר כ-`notes`).

**זיהוי ערוץ אוטומטי:** אימייל בתבנית `car-05XXXXXXXX@car.com` → `lead_source=רכב-לנדינג`, `product=vehicle_lien`. שאר טופס-אתר → לפי הגדרה (משכנתא/עסקי). ניתן לתקן ידנית ומייצר כלל.

**מיפוי לרמות המשפך שלנו:** `ליד חדש`/`חדשים לא נענו` → received · `אין מענה`/`לחשוב על זה`/`נשלחו דוחות` → contacted · `נסגר וחתם`/`לקוח נסגר ועבר ללקוחות` → signed · `לא רלוונטי`/`קמפיינים ישנים` → lost.

**ייבוא idempotent:** ריצה חוזרת מעדכנת סטטוסים ולא מכפילה. שדות שנערכו אצלנו (שכ"ט, חודש, תשלומים) לעולם לא נדרסים מייבוא.

**הגשות לבנקים** נכנסות לכרטיס התיק כטאב, ומזינות את ההסתברות: הגשה → 50%, "מאושר" → 70%+ (לפי סעיף 2.3). זה החיבור בין התפעול (WISE) לתחזית התזרים.

**שלב מאוחר (לא MVP):** אם ל-WISE יש API — החלפת הייצוא הידני בסנכרון לילי. שאלה פתוחה #14.

---

## 5. מסכים (מודולים)

| # | מסך | מה יש בו | הרשאה |
|---|---|---|---|
| 1 | **הבוקר** | 4 מספרים גדולים: יש עכשיו / מחויב החודש / צפוי ודאי / נקודה נמוכה ב-90 יום. עוגן להזנה (2 שדות). 3 התראות. | admin (+ partners צפייה מצומצמת ללא יתרת בנק) |
| 2 | **רווח והפסד** | מתג תפעולי/לחלוקה · מתג division · טווח תאריכים · drill-down · PDF/הדפסה/XLSX | admin, partners לפי division |
| 3 | **תזרים 13 שבועות** | גרף 2 קווים + טבלת שבועות + יומן חיובים 30 יום | admin |
| 4 | **תיקים (פייפליין)** | טבלה: לקוח, מוצר, שלב, סטטוס גביה, שכ"ט, נגבה, יתרה, ודאות היתרה, הוצאות ישירות, תרומה נטו, ערוץ. פילטרים: פוטנציאל / ודאי / חלקי. כרטיס תיק: לוח תקבולים, הוצאות ישירות (הוספה בלייב), היסטוריה. | admin, partner לפי division, backoffice |
| 5 | **תנועות** | כל התנועות, פילטרים, עריכה, סיווג bulk, "לשאול את…" | admin, backoffice |
| 6 | **הוצאות קבועות** | לוח החיובים, מאושר-50/50 כן/לא, "שולם לחודש X ✓" (התאמה לתנועה בפועל) | admin |
| 7 | **כרטיס ניסים** 🔒 | 8 שורות החישוב לכל חודש + יתרה מתגלגלת + מקדמות + סגירת חודש + PDF | admin, partner_finance |
| 8 | **משיכות שותפים נדל"ן** 🔒 | 33/33/33, משיכות מול יעד, חו"ז, חוב יוני, נכיון מזומן | admin |
| 9 | **הכנסות פרייבט** 🔒 | טבלת קרנות, חוקי סף, צפוי/התקבל, חלוקה דן/ניסים | admin, partner_finance |
| 10 | **מע"מ** | חבות בכל רגע, לפי תקופת דיווח, "תשומות ללא חשבונית" | admin, accountant |
| 11 | **ייבוא והתאמה** | העלאת אשראי/בנק/חשבונית ירוקה, מסך אישור, תור פערים | admin, backoffice |
| 12 | **פערי חשבוניות** | 3 הבדיקות מסעיף 4.3, שנה אחורה, ייצוא לרו"ח | admin, accountant |
| 13 | **תחזית רבעון** | טאב נדל"ן / טאב מימון / מאוחד · 3 תרחישים · הנחות ניתנות לדריסה · דיוק תחזית היסטורי | admin, partner לפי division |
| 14 | **לידים והמרה** | ייבוא מ-WISE (4.5) + השלמת עלויות; משפך ב-₪ לפי ערוץ; טבלה שבועית; היטמאפ; מסקנות | admin, backoffice |
| 15 | **תפעול (משימות)** | לוח משימות (לפי אחראי / תאריך / קשור-ל), משימות אוטומטיות מהמערכת, תת-משימות | כולם |
| 16 | **שאלות לשותפים** | תור "לשאול את ניסים/אביב" — השותף עונה מהנייד וסוגר | כולם |
| 17 | **קישורים** | כרטיסיות למערכות עבודה, לפי קטגוריה, ניתן לעריכה | כולם |
| 18 | **הגדרות** | רשימות סגורות, הסתברויות, מע"מ, מפתחות חלוקה, מיפוי עמודות ייבוא, כללים, משתמשים, סיסמאות אזורים, אחוז עלות מעביד, יום תשלום שכר | admin |
| 19 | **שכר עובדים** | הסכמים לעובד (בסיס + רכיבי בונוס), הזנה חודשית (שעות/פגישות/מכירות — חלק אוטומטי), תחשיב עם הסבר לכל שורה, אישור, PDF+XLSX לרו"ח, מעקב תשלום | admin (עובד: צפייה בתלוש-הסבר שלו בלבד — אופציונלי) |

**חוק המסכים:** אין מסך שמציג מספר שאי אפשר ללחוץ עליו ולראות את השורות שמרכיבות אותו.

### 5.1 רפרנס ויזואלי — הדשבורד של WISE (מה לוקחים ומה הטוויסט)

WISE היא הרפרנס לשפה הוויזואלית. **לא בונים WISE.** לוקחים את הדפוסים, מחליפים את התוכן מ"כמה בקשות" ל"כמה כסף".

| דפוס ב-WISE | מה זה אצלנו |
|---|---|
| **פס KPI עליון** (7 כרטיסים: לקוחות / מתעניינים / הגשות / סימולציות / טפסים / הכנסות) + בורר "מחודש–עד חודש" | פס של 6 כרטיסים בכל מסך ראשי, לפי division ותקופה: נגבה בפועל · פתוח לגביה ודאי · פוטנציאל משוקלל · הוצאות התקופה · רווח לחלוקה · יתרת ניסים (🔒). בורר תקופה גלובלי בראש הדף — משפיע על כל המסך, מחליף את "חודש נבחר" מהאקסל |
| **טאבים מתחת ל-KPI** (לקוחות / מתעניינים / הגשות / הכנסות) | טאבים: תיקים · תזרים · הוצאות · לידים · שותפות |
| **"תשלומים לפי חודשים"** — עמודות ירוקות + קו מגמה | "נגבה לפי חודש" — עמודות `actual` + קו committed+expected שמראה מה היה צפוי. הפער בין העמודה לקו = דיוק התחזית |
| **"סיכום גבייה שעדיין לא שולמה"** — עמודות אדומות לפי חודש | אותו גרף בדיוק, אבל **מפוצל לשני צבעים בעמודה:** יתרה ודאית (committed) / יתרה פוטנציאלית (expected). זה מה ש-WISE לא מבחינה בו |
| **"סה"כ הכנסות לפי שנים"** | "רווח לחלוקה לפי חודש" עם קו יעד (500K חודשי / 3M שנתי מההגדרות) |
| **משפך יחס המרה** (לא נסגר → מתעניין ראשוני → המשך מעקב → נקבעה פגישה → נסגר) | משפך שלנו: לידים → קשר → פגישה → חתם → נגבה. **עם ₪ בכל שלב:** עלות לידים בצד אחד, שכ"ט שנסגר ונגבה בצד השני. לפי ערוץ (טאב/פילטר) |
| **דונאט "סטטוס מתעניינים"** + **פאי "מקור פניה"** | דונאט סטטוס גביה (₪ לא כמות) + פאי ערוץ לפי ₪ שנגבה |
| **פילוח הגשות בבנקים** (פאי + עמודות לפי חודש + "אחוז המיצוי") | מיובא מ-WISE (4.5) ומוצג ככרטיס בתיק; ברמת דשבורד: "אחוז אישור לפי גוף מממן" — מזין את ההסתברות |
| **טבלת מתעניינים** — סטטוס כתגית צבעונית עם dropdown בשורה, פעילות אחרונה כטקסט, ייצוא לאקסל, פילטרים למעלה | תבנית לכל הטבלאות שלנו: תגיות סטטוס צבעוניות ניתנות לשינוי בשורה, ייצוא בכל טבלה, פילטרים בשורה עליונה, 10/25/50 בעמוד |
| **סטטוס בקשות** — טבלה: לקוח / בנק / סניף / תאריך הגשה / תאריך אישור / סטטוס | טאב "הגשות" בכרטיס תיק — מיובא |
| **כרטיס לקוח** — פרטים בראש, שורת טאבים (פרטים / הגשה / שכ"ט וביצוע / פגישות / חישוב זכאות / AI), **פיד פעילות** בצד עם חותמת מי-ומתי | כרטיס תיק שלנו: פרטים · לוח תקבולים · הוצאות ישירות (הוספה בלייב) · הגשות (מ-WISE) · מסמכים/חשבוניות · פיד פעילות (אוטומטי: כל תנועה, שינוי סטטוס, סגירת חודש שנגעה בתיק) |
| **תפריט צד ימני** עם אייקונים, לוגו למעלה | זהה. RTL, תפריט קבוע, מצב מכווץ למובייל |

**מה לא לוקחים:** סימולטור, מרכז מידע, מערכת טפסים, ביטוחים, כרטיסי ביקור, קריאת מסמכים ב-AI (שלב 2 אולי). ארבעת המסכים הראשונים בטבלה למעלה הם מה שנפתח כל יום; השאר נגישים מהתפריט.

**סגנון:** רקע לבן, כרטיסים עם צל עדין, כותרות ימין, מספרים גדולים ובולטים, ירוק=נכנס בפועל, כחול=ודאי צפוי, כתום=פוטנציאל, אדום=פתוח/חסר, אפור=נעול/היסטורי. ₪ עם פסיפי אלפים בכל מקום.

---

## 6. הרשאות ואבטחה

- אימות: אימייל + סיסמה (או Google). 2FA לאדמין.
- **סיסמה שנייה** (PIN) לאזורים 🔒 — נשאלת בכל כניסה לאזור, פג תוקף אחרי 10 דקות.
- Row-level: `partner_finance` רואה רק `division=finance`; `partner_realestate` רק `realestate`; `shared` מוצג לשניהם רק בחלק שלהם.
- **יתרת הבנק (עוגן) — אדמין בלבד.** השותפים רואים "צפוי להיכנס" ו"מחויב" ללא היתרה.
- `audit_log` על כל טבלה. מסך "מי שינה מה" לאדמין.
- אין שמירת פרטי גישה לבנק/אשראי. ייבוא קבצים בלבד.
- גיבוי: dump יומי (03:00) ל-Storage (90 יום) + XLSX/PDF יומי לתיקיית Drive `הר-אל/כספים/YYYY/MM` + עותק שבועי קר.

---

## 7. ייצוא ופלטים

| פלט | פורמט | מתי |
|---|---|---|
| רווח והפסד | PDF (RTL, לוגו), XLSX | לפי דרישה |
| דוח התחשבנות ניסים M/YYYY | PDF | אוטומטי בסגירת חודש |
| דוח לקוח למחזור ("מה תרם כל לקוח") | XLSX/PDF: לקוח, שכ"ט, נגבה, ישירות, תרומה, % מהמחזור, ערוץ | חודשי + לפי דרישה |
| פערי חשבוניות | XLSX לרו"ח | חודשי |
| **דוח שכר לרו"ח M/YYYY** | PDF + XLSX: לכל עובד — סוג העסקה, בסיס (שעות×תעריף), כל רכיב בונוס עם הכמות והתעריף, התאמות, **ברוטו לתלוש** / סכום לחשבונית, עלות מעביד משוערת, חלוקה לפעילות; שורת סיכום; חתימת מאשר ותאריך | אוטומטי באישור חודש השכר (כ-25 לחודש) — נשלח במייל לרו"ח + לדרייב |
| תזרים 13 שבועות | XLSX | לפי דרישה |
| כל טבלה | XLSX / Google Sheets (Drive API) / CSV | כפתור בכל טבלה |

PDF: RTL מלא, גופן עברי (Heebo/Assistant), כותרת עם ישות + תקופה + תאריך הפקה + "הופק ע"י".

---

## 8. המלצת ארכיטקטורה

**המלצה: Next.js (App Router, TypeScript) + Supabase (Postgres + Auth + RLS + Storage).**

למה לא Firestore (למרות שהר-אל OS עליו): המערכת הזו היא 90% התאמות, JOIN-ים, סכימות לפי תקופה ונעילות — זה SQL קלאסי. ב-Firestore כל דוח הופך לקוד ידני. Postgres נותן views, constraints, triggers ו-audit בשורה אחת. הר-אל OS ממשיך לחיות ב-Firebase; החיבור ביניהם דרך webhook (עסקה שנסגרת ב-CRM → POST ל-`/api/deals/from-crm`).

| רכיב | בחירה | הערה |
|---|---|---|
| DB | Supabase Postgres | RLS לפי division; `pg_cron` לסגירת יום |
| Backend | Next.js Route Handlers + Server Actions | לוגיקת חישוב ב-SQL views + TS |
| Frontend | Next.js + shadcn/ui + Tailwind (RTL) + Recharts/Tremor | מובייל-first למסך הבוקר, לשאלות ולמשימות |
| ייבוא קבצים | SheetJS (xlsx/csv), pdfplumber דרך Python microservice או `pdf-parse` + fallback OCR (Tesseract heb) | |
| PDF | Playwright (HTML→PDF) — RTL אמין | |
| Sheets | Google Sheets API (service account) | ייצוא + ייבוא גיליון לידים |
| WhatsApp | Green-API (קיים) | תזכורת עוגן יומית 08:30, שאלות לשותפים |
| Hosting | Vercel + Supabase cloud | ~₪0–150/חודש |
| גיבוי | Supabase PITR + cron ל-Drive | |

**חלופה** אם אתה מתעקש על Firebase: Firestore + Cloud Functions, אבל אז כל ה-views הופכים לפונקציות agg מתוחזקות ידנית. לא מומלץ למערכת התאמות.

**מבנה קוד:**
```
/app            מסכים (route per module)
/lib/rules      כללים עסקיים כטהורים (pure functions) — בדיקות יחידה על כל נוסחה בסעיף 3
/lib/import     parsers: isracard.ts, max.ts, cal.ts, bank_<x>.ts, greeninvoice.ts, pdf.ts
/lib/match      מנוע התאמה
/db/schema.sql  + migrations + views (v_pnl, v_nissim_card, v_cashflow_13w, v_vat, v_conversion)
/tests          fixtures: קובץ אשראי אמיתי מוסווה, דף בנק, ייצוא חשבונית ירוקה, 3 חודשי כרטיס ניסים מהקובץ הקיים — **התוצאות חייבות להשתוות לשקל** (יולי −28,262 / אוגוסט 105,882 / ספטמבר 22,447 רווח לחלוקה; יתרה 36,517)
```

---

## 9. תוכנית ביצוע — שלבים ואבני דרך

כל שלב מסתיים במשהו שמשתמשים בו. אין "בנייה שלושה חודשים ואז הפעלה".

| שלב | משך | תוצר | קריטריון סיום |
|---|---|---|---|
| **0 — מיפוי** | 3 ימים | רשימת חשבונות/כרטיסים, ~30 הוצאות קבועות עם יום ותוקף, רשימות סגורות, מפתחות חלוקה, יתרת פתיחה ניסים, חוב יוני, תשובות לשאלות הפתוחות | מסמך הגדרות חתום ע"י דן + ניסים |
| **1 — ליבה** | שבוע | סכימה + migrations + views + בדיקות יחידה על כל נוסחה | 3 חודשי כרטיס ניסים מהקובץ מתקבלים לשקל |
| **2 — תיקים + תנועות + הוצאות קבועות** | שבוע | מסכים 4, 5, 6 + הזנה ידנית + ייבוא חד-פעמי מהקובץ הקיים (28 עסקאות, הוצאות, מקדמות) | הקובץ הישן מוקפא לקריאה בלבד |
| **3 — כרטיס ניסים + P&L + PDF** | שבוע | מסכים 2, 7 + סגירת חודש + נעילה + PDF | סגירת ספטמבר 2026 נעשית במערכת |
| **4 — ייבוא אשראי + כללים** | שבוע | 4.1 + 4.4 + מסך 11 (אשראי בלבד) | קובץ אשראי אמיתי מסווג 80%+ אוטומטית אחרי חודש |
| **5 — עוגן + תזרים + התראות** | שבוע | מסכים 1, 3 + סגירת יום + Green-API | דן מזין יתרה מהנייד יומית; התראה ראשונה נתפסה |
| **6 — בנק + חשבונית ירוקה + מע"מ + פערים** | 1.5 שבועות | 4.2, 4.3, מסכים 10, 12 | דוח פערים 12 חודשים אחורה מופק ונשלח לרו"ח |
| **7 — נדל"ן: משיכות, חו"ז, פרייבט** | שבוע | מסכים 8, 9 + PIN | אביב ויוני רואים את חלקם |
| **8 — לידים + תחזית + WISE** | שבוע | מסכים 13, 14 + ייבוא WISE (מתעניינים, לקוחות, הגשות) | 277 המתעניינים ו-35 ההגשות מ-WISE במערכת; דוח המרה שבועי ראשון ב-₪ |
| **8b — שכר** | 4 ימים | מסך 19 + דוח לרו"ח + חיבור לתזרים ול-P&L | שכר ספטמבר/אוקטובר 2026 מחושב במערכת ונשלח לרו"ח ממנה |
| **9 — תפעול, שאלות, קישורים, גיבוי** | 4 ימים | מסכים 15, 16, 17 + cron גיבוי + Drive | ניסים עונה לשאלה מהנייד; גיבוי מגיע לדרייב |
| **10 — הקשחה** | 3 ימים | audit, RLS, 2FA, בדיקות קצה, מדריך שימוש קצר | 30 יום ללא אקסל מקביל |

סה"כ ~8–9 שבועות עבודה נטו. ערך מהשלב 3 (אחרי ~3 שבועות).

---

## 10. שאלות פתוחות — לענות לפני שלב 1

1. **בנק וכרטיסים:** באיזה בנק החשבון? אילו חברות אשראי (ישראכרט/מקס/כאל) ואילו כרטיסים משמשים את ניסים אישית? (קובע את ה-parsers.)
2. **חשבונית ירוקה:** איזה ייצוא זמין לך (דוח מסמכים / דוח הכנסות / API)? מוציאים חשבוניות מכמה ישויות (החברה, D&D, עדן)?
3. **הוצאות קבועות "מאושרות":** מי מאשר ומה קורה עם הוצאה קבועה לא מאושרת — יורדת רק מחלק הר-אל? (במודל שלמעלה: `approved_by_nissim=false` → נכנסת לרווח התפעולי אבל לא לרווח לחלוקה, כלומר הר-אל סופגת 100%.)
4. **מקדמות באשראי:** מקדמה שניסים מושך בכרטיס — נספרת בברוטו (כולל מע"מ), נכון? והאם החברה מקבלת מע"מ תשומות על חלק מזה (לא אמורה — זה פרטי)?
5. **חודש הפסד:** האם ניסים נושא ב-50% מהפסד (החוב שלו גדל), כפי שהקובץ הקיים עושה? זה צריך הסכמה מפורשת.
6. **שיוך חודש:** מסכים על "חודש = תקבול ראשון, תקבול שני נספר בחודש שנכנס"? או שרוצים חודש חתימה?
7. **הכנסות פרייבט:** כללי הסף לכל קרן — כדאי טבלה: קרן / סף חודשי / אחוז / מי מקבל.
8. **יוני 200,000 ₪:** מה מקור החוב (הלוואת בעלים / השקעה) ומה קצב ההחזר המוסכם? האם ההחזר יוצא לפני חלוקת רווחים או מתוך חלקו?
9. **שכר יוני+נדיה:** האם עלות המעביד המלאה נספרת כמשיכה של יוני (כפי שהנחתי) או שהחברה סופגת את עלות המעביד כהוצאה כללית?
10. **נכיון מזומן:** לרשום כהכנסה רגילה עם דגל, או מחוץ לספרים? (המערכת תרשום — ההחלטה החשבונאית של הרו"ח.)
11. **הדס ושירותים משותפים:** מפתח 80/20 — האם יש עוד הוצאות משותפות עם מפתח אחר (משרד? תוכנה?).
12. **גיליון הלידים הקיים:** להשאיר את ההזנה שם ולייבא, או להעביר את ההזנה למערכת?
13. **Firebase או Supabase** — לפי סעיף 8.
14. **WISE:** האם יש API / ייצוא מתוזמן, או רק ייצוא ידני לאקסל? האם הלקוחות של נדל"ן מנוהלים גם שם או רק ב-CRM של הר-אל OS? (קובע אם הייבוא ב-4.5 הוא ידני-שבועי או אוטומטי.)
15. **חשבוניות D&D לחברה:** מה בדיוק מכסה "דמי ניהול הר-אל כולל ניסים" — דן מוציא חשבונית אחת גם על החלק של ניסים? אם כן, איך זה נרשם בהתחשבנות (העברה לניסים אחר כך = `transfer`?). וכיצד ניסים ואביב מחייבים — חשבונית לחברה או משיכה ישירה?
16. **שכר — לכל עובד:** רשימת העובדים והסכם כל אחד (תעריף/בסיס, רכיבי בונוס וכמויות), מי בתלוש ומי בחשבונית, איך נספרות "פגישות" (מ-WISE / ידני / מהיומן) ומה נחשב "מכירה" לצורך בונוס (חתימה או גבייה?), ואיזה פורמט הרו"ח רוצה לקבל (יש לו תבנית?).

---

## 11. הנחיות ל-Claude Code (להדבקה ב-CLAUDE.md של הריפו)

```
אתה בונה מערכת כספים פנימית לפי HAREL_FINANCE_SPEC.md. כללים:
1. הספק (SPEC) מחייב. סעיפים 1, 2, 3 אינם ניתנים לפרשנות — כל סטייה דורשת אישור מפורש מדן.
2. עבודה בשלבים לפי סעיף 9. אין להתחיל שלב N+1 לפני שקריטריון הסיום של N עובר.
3. כל נוסחה בסעיף 3 = פונקציה טהורה ב-/lib/rules עם בדיקת יחידה. ה-fixtures בסעיף 8 חייבים לעבור לשקל.
4. כל טבלה: created_at, updated_at, created_by; טריגר audit_log. אין DELETE פיזי — soft delete.
5. סכומים: NUMERIC(14,2). לעולם לא float. מע"מ מחושב פעם אחת בכתיבה ונשמר.
6. UI: עברית RTL מלאה, מובייל-first למסכים 1, 15, 16. אין אנגלית במסך.
7. אין מסך שמציג מספר בלי drill-down לשורות המרכיבות.
8. תקופה נעולה: כל UPDATE/INSERT עם period closed נדחה ברמת DB (trigger), לא רק ב-UI.
9. ייבוא: idempotent. ריצה חוזרת על אותו קובץ = 0 שורות חדשות.
10. לפני כל migration הרסני — גיבוי + הודעה לדן.
11. אין להמציא נתוני דמו כמספרים אמיתיים. seed = הנתונים מהקובץ הקיים בלבד.
12. בכל PR: מה נבנה, איזה קריטריון מסעיף 9 הוא מקדם, מה נשאר פתוח.
13. עיצוב לפי סעיף 5.1 (רפרנס WISE). WISE נשארת מקור האמת ללידים/לקוחות/הגשות — לא לבנות מסכי הזנה מקבילים להם.
```

---

## נספח א — מיפוי מהקובץ הקיים (הר-אל פתרונות מימון 2026) למערכת

| גיליון/עמודה קיים | יעד במערכת |
|---|---|
| עסקאות · K הוצאות ישירות | `transactions` עם `deal_id`, `nature=expense`, `category.kind=direct` |
| עסקאות · O מיקום בתהליך | `deals.stage` |
| עסקאות · P שכ"ט נסגר | `deals.fee_agreed_net` |
| עסקאות · R סטטוס פתוח | `deals.collection_status` (רשימה מתוקנת) |
| עסקאות · S חודש | `deals.month_attributed` |
| עסקאות · U נגבה בפועל | **נגזרת** — Σ `income actual` עם `deal_id` (מתקן ליקוי #1, #3) |
| עסקאות · V פתוח לגביה | נגזרת + `deal_payments_plan` עם ודאות |
| הוצאות · E מוכרת למס | `transactions.deductible` |
| הוצאות · C קטגוריה | `category_id` (חובה — אין "נעלמות") |
| מקדמות | `advances` |
| הגדרות · B8 אחוז ניסים | `partners.share_pct` |
| הגדרות · K–L מיפוי נגבה? | מוחלף בתנועות בפועל — אין יותר "כן/לא" שקובע כסף |
| כרטיס ניסים | `v_nissim_card` view + `periods` |
| תקציב | `budgets` (קטגוריה × חודש) — מסך 2 בהמשך; לא ב-MVP |

## נספח ב — 9 הליקויים שהמערכת סוגרת מבנית
1. נוסחאות שנמחקות → אין נוסחאות בתאים, רק views.
2. עסקה בלי חודש נעלמת → `month_attributed` נדרש לסגירת חודש (חריג חוסם).
3. תקבול חלקי נספר כמלא → נגבה = Σ תנועות בפועל.
4. טווח 44 עסקאות → אין טווחים.
5. חודש נבחר ידני → בורר תקופה בכל מסך, YTD תמיד נכון.
6. פילוח שיווק לא מחובר → `lead_costs` הן תנועות בפועל.
7. הוצאה בלי קטגוריה → שדה חובה + כלל.
8. תאריכי גביה מומצאים → `deal_payments_plan` עם ודאות מפורשת.
9. רווח תפעולי ≠ לחלוקה בלי הסבר → מתג עם כותרת ו-drill-down.
