# שאלות פתוחות — לענות לפני שלב 1 מלא

15 השאלות מ-SPEC §10, עם מה שהמערכת מניחה **כרגע** ואיפה בקוד לשנות.
כל הנחה כאן היא ברירת מחדל שנבחרה כדי לאפשר התקדמות — לא הכרעה.

| # | שאלה | ההנחה בקוד | איפה לשנות | חוסם? |
|---|---|---|---|---|
| 1 | באיזה בנק החשבון? אילו חברות אשראי? | טרם נבחר | `lib/import/` — ה-parsers טרם נכתבו | חוסם שלב 4 ו-6 |
| 2 | איזה ייצוא זמין מחשבונית ירוקה? כמה ישויות מוציאות חשבוניות? | 3 ישויות (`entities`), ייצוא CSV 14 עמודות כמתועד ב-§4.3 | `db/schema/002_core.sql` `entities` | חוסם שלב 6 |
| 3 | הוצאה קבועה לא מאושרת — מי סופג? | הר-אל סופגת 100%: נכנסת לרווח התפעולי, לא לחלוקה | `lib/rules/pnl.ts` `selectDistributableExpenses`; `db/views/002_reports.sql` `v_pnl_distributable` | לא |
| 4 | מקדמה באשראי — ברוטו? ומע"מ תשומות? | ברוטו. אין קיזוז מע"מ (זה שימוש פרטי) | `advances.amount_gross`; `lib/rules/nissim-card.ts` שורה 6 | לא |
| 5 | חודש הפסד — ניסים נושא ב-50%? | **כן** (`lossSharing: 'shared'`), כמו בקובץ הקיים | `lib/rules/nissim-card.ts` `LossSharing` — יש כבר חלופה `harel_absorbs` | **דורש הסכמה מפורשת** |
| 6 | שיוך חודש — תקבול ראשון או חודש חתימה? | תקבול ראשון, ותקבול שני נספר בחודש שנכנס | `lib/rules/attribution.ts`; `db/views/001_base.sql` `v_deal_month` | לא |
| 7 | כללי הסף לכל קרן בהכנסות פרייבט | `private_income.threshold_rule` הוא jsonb פתוח — אין חוקים מוגדרים | `db/schema/004_settlement.sql` | חוסם שלב 7 |
| 8 | חוב יוני 200,000 — מקור וקצב החזר? לפני או מתוך חלקו? | נרשם כ-`owner_loans` נפרד; ההחזר **לא** נספר כמשיכה מול היעד | `lib/rules/realestate.ts` `DRAW_TYPES_COUNTED` | חוסם שלב 7 |
| 9 | שכר יוני+נדיה — עלות המעביד נספרת כמשיכה שלו? | **כן**, עלות התלוש המלאה | `partner_draws.includes_employer_cost`; `lib/rules/realestate.ts` | חוסם שלב 7 |
| 10 | נכיון מזומן — הכנסה עם דגל או מחוץ לספרים? | הכנסה רגילה עם `cash_discount=true`, מוצגת בנפרד | `transactions.cash_discount` | לא |
| 11 | הדס 80/20 — יש עוד הוצאות משותפות במפתח אחר? | 80/20 כברירת מחדל גלובלית; כל שורה יכולה לדרוס | `settings.default_division_split` | לא |
| 12 | גיליון הלידים — להשאיר ולייבא או להעביר למערכת? | ייבוא (WISE נשארת מקור האמת התפעולי, §4.5) | `lib/import/wise.ts` — טרם נכתב | חוסם שלב 8 |
| 13 | Firebase או Supabase? | **Postgres** — הסכימה כתובה ל-Postgres 16 | כל `db/` | הוכרע בקוד; שינוי = כתיבה מחדש |
| 14 | ל-WISE יש API או רק ייצוא ידני? | ייצוא ידני; `wise_ref` מוכן לשידוך idempotent | `leads.wise_ref`, `deals.wise_ref`, `deal_submissions.wise_ref` | חוסם שלב 8 |
| 15 | "דמי ניהול הר-אל כולל ניסים" — מה זה מכסה? | ספק שהוא ישות של שותף → `nature=draw`, דורש אישור ידני | `suppliers.is_partner_entity` | **חוסם** — משפיע ישירות על כרטיס ניסים |

## שאלות 17–22 מה-ADDENDUM

| # | שאלה | ההנחה בקוד | איפה לשנות | חוסם? |
|---|---|---|---|---|
| 17 | חשבונית ירוקה API — יש גישה? מה כתובת קליטת ההוצאות במייל? | ייבוא ידני; `invoices.source='greeninvoice_import'` מוכן | `lib/import/greeninvoice.ts` — טרם נכתב | חוסם ב.6 ("הוצא חשבונית") |
| 18 | לאיזו תיבה מגיעות החשבוניות — דן / הדס / office@? | טבלת `integrations` תומכת בכמה חשבונות (`account_label`) | `integrations` | **חוסם ב.3** |
| 19 | הרו"ח — כתובת, מה בדיוק ב-12 לחודש, האם יש תבנית | `report_runs.recipients` ריק; אין תבנית | `report_runs`, `month_close_checklist` | חוסם ב.7 |
| 20 | יומן משני "הר-אל כספים" או היומן הראשי? | יומן משני (ההמלצה ב-ב.1) | `integrations.scopes` | לא |
| 21 | סיכום יומי לשותפים — רוצים? באיזו רמת פירוט? | גרסה מצומצמת לפי §6: ניסים מימון בלבד, אביב נדל"ן, ללא יתרת בנק | `outbox` + שכבת הדוחות | לא |
| 22 | **ספי התראות** — סטיית יום, יתרת ניסים, מע"מ מפוספס | 1,500 ₪ · 50,000 ₪ · 1,000 ₪ · 48 שעות · 5 ימים · ±20% · 5 קריטיות | `lib/rules/alerts.ts` `DEFAULT_THRESHOLDS` | **טעון אישור** — ספים שגויים הופכים התראה לרעש |

הספים ב-`DEFAULT_THRESHOLDS` הם ניחוש מושכל שנבחר כדי לאפשר התקדמות. §ב.11 קובע
ש"התראה = משהו שדורש החלטה, לא רעש" — ולכן דווקא כאן מספר לא נכון הורס את התכונה.

---

## שאלה נוספת שעלתה במימוש

**16. הוצאה משתנה מוכרת שאינה רשומה כהוצאה קבועה ואינה ישירה — נכנסת לרווח לחלוקה?**

§3.2 מונה שלוש שורות בלבד: הכנסות, הוצאות קבועות, הוצאות ישירות. הוצאה משתנה
(`category.kind='variable'`) לא מופיעה בטבלה. ההנחה בקוד: **נכנסת**, אם `deductible=true` —
כי זו עלות תפעולית אמיתית של פעילות המימון, ואי-הכללתה תנפח את הרווח לחלוקה.

בקוד: `lib/rules/nissim-card.ts` שורה 2, ו-`v_pnl_distributable` בתנאי `c.fixed_expense_id is null`.
אם התשובה "לא" — התנאי משתנה ל-`and c.fixed_expense_id is not null`.

**17. עמלת יזם בחודש הראשון של התחזית (§3.7.1)**

§3.7.1 קובע שעמלת היזם מגיעה net-30 — "בחודש +1". בחודש הראשון בתחזית אין עדיין
עסקאות חדשות שקדמו לו, ולכן הקוד מחזיר 0: העמלות מעסקאות שכבר נחתמו יושבות
בפייפליין הקיים (`deal_payments_plan`) ונספרות משם. אם בפועל אין להן שורות
פייפליין — הן ייעלמו מהתחזית. **לאמת שכל עסקת נדל"ן פתוחה עם עמלת יזם צפויה
מקבלת שורת `deal_payments_plan`.**

בקוד: `lib/rules/forecast.ts`, `computeRealEstateForecast`, התנאי `i > 1`.

---

## המגבלה הגדולה: הפיקסצ'ר אינו הקובץ האמיתי

קריטריון הסיום של שלב 1 הוא "3 חודשי כרטיס ניסים מהקובץ מתקבלים לשקל".

**מה עובר כרגע:** הנוסחאות מפיקות בדיוק את היעדים מ-§8
(יולי −28,262 · אוגוסט 105,882 · ספטמבר 22,447 · יתרה 36,517).

**מה לא נבדק:** הקובץ "הר-אל פתרונות מימון 2026" לא היה זמין. שורות ההכנסה וההוצאה
ב-`tests/fixtures/nissim-card-2026.ts` הן **שחזור** שנבנה כדי להגיע ליעדים האלה —
לא ייצוא מהקובץ. יתרת הפתיחה (29,550.50) והמקדמות (19,000 לחודש) נגזרו לאחור
מהיתרה הסופית, ולא אומתו.

**כדי לסגור:** לייצא מהקובץ הקיים את שורות שלושת החודשים, להחליף את גוף הפיקסצ'ר,
ולוודא שהבדיקות עדיין עוברות. אם הן נופלות — הנוסחה או הקובץ טועים, וזה בדיוק
מה שהבדיקה נועדה לתפוס. עד אז: **שלב 1 עובר על שחזור, לא על הנתונים האמיתיים.**

בנוסף, §3.3 מציין שיתרת הפתיחה האמיתית היא "~36,517 ₪ נכון לספטמבר, לאמת מולו" —
כלומר גם המספר הזה טעון אישור של ניסים לפני שהוא נכנס כ-`periods.opening_balance`.
