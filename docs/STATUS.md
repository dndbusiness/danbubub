# מצב הביצוע — לפי SPEC §9

| שלב | תוצר | קריטריון סיום | מצב |
|---|---|---|---|
| **0 — מיפוי** | רשימות סגורות, הוצאות קבועות, יתרות פתיחה, תשובות ל-15 השאלות | מסמך הגדרות חתום ע"י דן + ניסים | **⚠ פתוח** — `docs/OPEN_QUESTIONS.md` |
| **1 — ליבה** | סכימה + views + בדיקות יחידה על כל נוסחה | 3 חודשי כרטיס ניסים מתקבלים לשקל | **✔ הושלם על הקובץ האמיתי** — יולי ואוגוסט לשקל; ספטמבר עם הפרש מתועד (ראו למטה) |
| **1b — עדכון ל-SPEC v2 + ADDENDUM** | §3.7 חדש, צ'קליסט ביצוע, גביה, התראות, סכימת הנספח | הנוסחאות והטבלאות החדשות עם בדיקות | **✔ הושלם** |
| **2 — תיקים + תנועות + הוצאות קבועות** | `/ui-kit` + 10 הקומפוננטות + מעטפת + מסכים 4, 5, 6 + הזנה ידנית + **ייבוא מהקובץ הקיים** | הקובץ הישן מוקפא לקריאה בלבד | **✔ הייבוא רץ ו-idempotent; ההקפאה — פעולה של דן ב-Google Sheets** |
| **3 — כרטיס ניסים + P&L + PDF** | מסכים 2, 7 + סגירת חודש + נעילה + PDF | סגירת ספטמבר 2026 במערכת | **◐ המנגנון בנוי ונבדק מקצה לקצה; ספטמבר חסום בנתונים (7 תיקים בלי חודש) ובהכרעה #23** |
| **4 — ייבוא אשראי + כללים** | 4.1, 4.4, מסך 11 | 80%+ סיווג אוטומטי אחרי חודש | **◐ המנגנון בנוי ונבדק מקצה לקצה על קבצים סינתטיים; המדד נמדד במסך. הקריטריון נמדד רק אחרי חודש של קבצים אמיתיים (שאלה #1)** |
| **5 — עוגן + תזרים + התראות** | מסכים 1 (21), 3 + סגירת יום + Green-API | דן מזין יתרה מהנייד יומית; התראה ראשונה נתפסה | **◐ בנוי ונבדק מקצה לקצה; ההתראה הראשונה נתפסה על ה-DB המקומי. הקריטריון האמיתי — דן מזין בפועל יום אחרי יום, ומפתחות Green-API בשרת** |
| **6 — בנק + חשבונית ירוקה + מע"מ** | 4.2, 4.3, מסכים 10, 12 | דוח פערים 12 חודשים | **◐ בנוי ונבדק מקצה לקצה** — הדוח הופק (PDF) ונרשם ב-`report_runs`; השליחה בפועל ממתינה ב-outbox עד שגוגל מחובר |
| **7 — נדל"ן** | מסכים 8, 9 + PIN | אביב ויוני רואים את חלקם | **◐ בנוי ונבדק מקצה לקצה** — כל שותף רואה יעד, משיכה וסטייה משלו. אין עדיין תנועות נדל"ן אמיתיות בקובץ, ולכן המסך מלא מבנה וריק מנתונים |
| **8 — לידים + תחזית + WISE** | מסכים 13, 14 + ייבוא WISE | דוח המרה שבועי ראשון ב-₪ | **◐ בנוי ונבדק מקצה לקצה על קבצים סינתטיים** — המשפך ב-₪ עובד; הקריטריון האמיתי (277 מתעניינים, 35 הגשות) דורש את הייצואים מ-WISE |
| **8b — שכר** | מסך 19 | שכר נשלח לרו"ח מהמערכת | לא התחיל |
| **9 — תפעול, שאלות, קישורים, גיבוי** | מסכים 15, 16, 17 | גיבוי מגיע לדרייב | לא התחיל |
| **10 — הקשחה** | audit, RLS, 2FA | 30 יום ללא אקסל מקביל | לא התחיל |

---

## מה נבנה בשלב 1

### נוסחאות §3 — כל אחת פונקציה טהורה עם בדיקות

| §  | נושא | מודול | בדיקות |
|---|---|---|---|
| 1.4 | פירוק מע"מ (`excl`/`incl`/`exempt`) | `lib/rules/vat.ts` | 13 |
| 3.1 | חבות מע"מ + תשומות ללא חשבונית | `lib/rules/vat.ts` | ↑ |
| 3.2 | רווח תפעולי מול רווח לחלוקה | `lib/rules/pnl.ts` | 9 |
| 3.3 | כרטיס ניסים — 8 שורות + חריגים חוסמים | `lib/rules/nissim-card.ts` | 29 |
| 3.4 | שיוך עסקה לחודש | `lib/rules/attribution.ts` | ↑ |
| 3.5 | נדל"ן 33/33/33, חו"ז, חוב יוני, נכיון | `lib/rules/realestate.ts` | 9 |
| 3.6 | תזרים 13 שבועות, סגירת יום, עוגן | `lib/rules/cashflow.ts` | 14 |
| 3.7 | **תחזית: מנוע נפרד לכל פעילות + מאוחד + דיוק היסטורי** | `lib/rules/forecast.ts` | 29 |
| 3.8 | יחס המרה שבועי + מסקנות אוטומטיות | `lib/rules/conversion.ts` | 10 |
| 3.9 | תחשיב שכר + פרו-רטה + הסברים | `lib/rules/payroll.ts` | 14 |
| 2.3 | הסתברות לפי שלב + רקב | `lib/rules/probability.ts` | 15 |
| 1.2 | הפרדת פעילויות ומפתחות חלוקה | `lib/rules/division.ts` | 9 |
| 11.5 | חשבון כספים באגורות שלמות | `lib/rules/money.ts` | 17 |
| 1.6 | תקופות ותאריכים | `lib/rules/period.ts` | 10 |
| ב.5 | צ'קליסט ביצוע — "מה חסר כדי לקבל כסף" | `lib/rules/checklist.ts` | 19 |
| ב.6 | גביה, גיול, דגלים אוטומטיים | `lib/rules/collections.ts` | 20 |
| ב.11 | מנוע התראות — 17 כללים, מצב סיכון | `lib/rules/alerts.ts` | 28 |

**סה"כ 245 בדיקות, כולן עוברות.**

### סכימה — 38 טבלאות, כל ההבטחות נאכפות ב-DB

`./scripts/verify-db.sh` מרים Postgres נקי, מריץ את הסכימה וה-views, ואז **מנסה להפר**
כל הבטחה מ-§11 ונכשל אם ההפרה מצליחה:

```
✓ §11.5 מע"מ מחושב ונשמר בכתיבה        ✓ §11.4 אין DELETE פיזי
✓ §2.1  advance מוגבל ל-finance          ✓ §1.7  יומן שינויים נכתב
✓ §2.1  draw דורש שותף                   ✓ §11.8 תקופה נעולה נדחית ברמת ה-DB
✓ נספח ב #7 הוצאה חייבת קטגוריה          ✓ §11.9 ייבוא idempotent
✓ §1.2  shared חייב מפתח חלוקה           ✓ §3.9  תקופות הסכם לא חופפות
✓ §2.1  סימן הסכום תואם לטבע התנועה      ✓ §2.1  תקופה סגורה חייבת תצלום
```

ואותו דבר להבטחות ה-ADDENDUM:

```
✓ הנחיה 17 verified דורש אישור אנושי     ✓ ב.5    פריט חסום חייב סיבה
✓ ב.3    סריקת מייל idempotent            ✓ ב.5    סדר הפריטים בתיק ייחודי
✓ הנחיה 18 התראה אחת לכל בעיה             ✓ §3.7.3 תצלום אחד לכל אופק
✓ הנחיה 18 התראה שנסגרה יכולה לחזור       ✓ §3.7.3 אופק מוגבל ל-30/60/90
✓ הנחיה 16 outbox מונע שליחה כפולה        ✓ ב.9    תקציב אחד לכל חודש
✓ הנחיה 15 אין עמודת סוד ב-integrations   ✓ §1.7   הטבלאות החדשות ביומן
```

**24 הבטחות מבניות, כולן נבדקות בכל הרצה.**

### Views — כל דוח נגזר, אין נוסחה בתא

`v_pnl` · `v_nissim_card` · `v_vat` · `v_cashflow_items` · `v_conversion` ·
`v_deal_balance` · `v_invoice_gaps` · `v_tx_allocated` · `v_deal_month` ·
`v_collections` · `v_collections_aging` · `v_execution_pipeline` ·
`v_execution_blockers` · `v_active_alerts` · `v_risk_mode` · `v_system_health`

ה-views וה-`lib/rules` מוזנים **מאותו פיקסצ'ר** (`scripts/fixture-to-sql.mjs`)
ונבדקים זה מול זה, כדי שאי אפשר יהיה לתקן אחד ולשכוח את השני:

```
✓ 2026-07 רווח לחלוקה = -28,262      ✓ יתרת ניסים סוף ספטמבר = 36,517
✓ 2026-08 רווח לחלוקה = 105,882      ✓ v_pnl מתלכד עם שורה 4 בכרטיס ניסים
✓ 2026-09 רווח לחלוקה = 22,447       ✓ §1.2 חלוקת shared לא מאבדת ולא מכפילה
✓ §2.1 private לא דולף לדוחות         ✓ §3.4 תקבול שני נספר בחודש שנכנס
```

### תשעת הליקויים מנספח ב

| # | הליקוי | איך נסגר |
|---|---|---|
| 1 | נוסחאות שנמחקות | אין נוסחאות בתאים — רק views |
| 2 | עסקה בלי חודש נעלמת | `findClosingBlockers` חוסם סגירה; `v_deal_month` גוזר אוטומטית |
| 3 | תקבול חלקי נספר כמלא | `v_deal_balance.collected_net` = Σ תנועות בפועל |
| 4 | טווח 44 עסקאות | אין טווחים — שאילתות |
| 5 | חודש נבחר ידני | בורר תקופה בכל שאילתה |
| 6 | פילוח שיווק לא מחובר | `lead_costs` הן תנועות עם `tx_id` |
| 7 | הוצאה בלי קטגוריה | `CHECK` ב-DB, נבדק |

*(ליקויים 8–9 לא פורטו במסמך המקור.)*

---

## שלב 2 — מה נבנה

**תשתית UI:** Next.js 15 (App Router) + Tailwind v4, tokens של UIUX §2 כ-CSS variables
(`app/globals.css`, `:root` + `.dark`), Heebo, RTL מלא, tabular-nums לכל מספר.

**10 הקומפוננטות (UIUX הנחיה 22):** `<Money>` · `<KpiCard>` · `<StatusPill>` · `<DrillDrawer>` ·
`<DataTable>` · `<PeriodPicker>` · `<DivisionSwitch>` · `<PinGate>` · `<StaleBadge>` · `<RiskBanner>` —
כולן ב-`/ui-kit` בכל המצבים (הנחיה 23). התקופה והפעילות חיות ב-URL (`?period=&division=`).

**מעטפת (UIUX §3):** תפריט ימני 240/64px זוכר מצב, מכווץ בטאבלט; פס עליון עם בורר תקופה
ומתג פעילות; ניווט תחתון 5 פריטים בנייד; טבלאות הופכות לכרטיסים מתחת ל-768.

**מסכים:**
| # | מסך | מה יש |
|---|---|---|
| 4 | תיקים — מימון | 6 KPI עם drill-down · טאבים (הכל/פוטנציאל/ודאי/חלקי/לביצוע וגביה/הושלם) · טבלה לפי UIUX §5.2 · תיק חדש (יוצר צ'קליסט מהתבנית) |
| 4 | תיקים — נדל"ן | **כסף בלבד** (החלטה 3): לקוח, סכום עסקה, 2% שכ"ט, 1% עמלת יזם שוטף+30, פוטנציאל/סגור, הוצאה ישירה |
| 4′ | כרטיס תיק | ראש + 4 מספרים · טאבים: תקבולים (לוח + בפועל) · הוצאות ישירות (+הוספה) · צ'קליסט (ADDENDUM ב.5) · הגשות/מסמכים (placeholder) · פיד פעילות מ-`audit_log` |
| 5 | תנועות | 6 KPI · טבלה עם **עריכה בשורה** (קטגוריה, מוכרת, חשבונית בלחיצה, סקירה) · שורה נעולה אפורה · תנועה חדשה עם שדה סכום חי (ללא/מע"מ/כולל) ו-slider למשותף |
| 6 | הוצאות קבועות | 4 KPI · **כרטיס 12 חודשים** לכל הוצאה (ADDENDUM ב.9: ✓ ירד / ✗ לא ירד / ⚠ סכום שונה / צפוי) · מתגי מאושר 50/50 ופעיל · הוצאה חדשה |

**נתונים:** `lib/queries/*` קורא מה-views; `app/actions/*` כותב דרך `withActor` (מגדיר
`app.current_user_id` לטריגר ה-audit). המע"מ מחושב ב-DB. הנעילה נאכפת ב-DB ומוסברת ב-UI.

**כלים:** `./scripts/dev-db.sh start` — Postgres מקומי מתמיד עם סכימה, views, קטגוריות
ברירת מחדל ו-seed משוחזר. `node scripts/ui-screenshots.mjs` — UIUX הנחיה 24: כל מסך
ב-390/768/1280, נכשל על שגיאות עמוד וגלילה אופקית. הצילומים ב-`docs/screenshots/`.

**הייבוא מהקובץ הקיים (`scripts/import-workbook.mjs`):**
`lib/import/workbook.ts` הוא parser טהור לקובץ "הר-אל-פתרונות-מימון-עסקי-דשבורד-ניסים-2026"
(SheetJS, לפי §8). 28 תיקים · 38 תנועות · 6 מקדמות · 18 שורות לוח תקבולים · 30 הגשות · 8 שורות
פרייבט. idempotent (§11.9): hash של הקובץ ב-`import_batches` + `source_ref` יציב לכל שורה —
ריצה שנייה = 0 שורות חדשות. הקובץ המוסווה (שמות → "לקוח NN", סכומים כפי שהם) ב-
`tests/fixtures/workbook/`, והפיקסצ'ר של שלב 1 נוצר ממנו אוטומטית (`scripts/fixture-from-workbook.mjs`).

**מה חסר בשלב 2:**
- הזנה מהירה בנייד (➕ sheet, UIUX §3.2) — הכפתור קיים, ה-sheet לא.
- חיפוש גלובלי ⌘K, "הפוך לכלל?" אחרי סיווג (§4.4), מצב כהה (tokens קיימים, אין מתג).
- בחירה מרובה + סרגל פעולות בטבלה (UIUX §4.5).

## מה הייבוא מצא בקובץ

| ליקוי (נספח ב) | מה בקובץ | מה המערכת עושה |
|---|---|---|
| #1 נוסחה שנמחקה | `כרטיס ניסים!E10` (הוצאות ישירות יולי = 4,862) הוא **מספר מוקלד**, בלי שורות מאחוריו | מיובא כהוצאה בלי תיק, `ask_nissim`; הרווח תואם לקובץ, הפער גלוי |
| #2 עסקה בלי חודש נעלמת | 3 תקבולים (רחלי 2,300 · יעקוב 10,160 · מיסא 5,000 = **17,460**) עם "נגבה בפועל" ובלי חודש — `SUMIFS` לא סופר אותם | מיובאים בתאריך הייבוא, `ask_nissim`. ספטמבר במערכת **38,322** ולא 22,447; יתרת ניסים **28,579** ולא 36,516.5 |
| — | "הוצאות מוכרות" = עמודה `מוכרת?`="כן"; **הדס מסומנת "לא"** (9,450 ביולי, 13,500 באוגוסט) | `deductible=false` — לא נכנסת לרווח לחלוקה. לאשר שזו הכוונה |
| — | 3 סכומי "נגבה" שונים בקובץ: 154,600 (עסקאות!R56) · 164,979 (כרטיס) · 182,439 (Σ עמודה U) | המערכת: 182,439 — סכום התנועות |
| — | הוצאות ישירות כטקסט ("36 ש"ח", "67 ש"ח") | מנורמלות למספר, אזהרה |
| — | "מתנה לחג" מתוארכת 09/06 אבל בחודש 9 | חודש הקובץ גובר; אזהרה |

**ההפרש בספטמבר (15,875 ₪ = 17,460 − 1,585 הוצאות ישירות של אותם תיקים) הוא הכרעה של דן וניסים** —
באיזה חודש להכיר בתקבולים האלה — לא באג. עד להכרעה הם מסומנים "לשאול את ניסים" ומופיעים בכרטיס ספטמבר.

## שלב 3 — מה נבנה

| # | מסך | מה יש |
|---|---|---|
| 7 | **כרטיס ניסים 🔒** | PIN (SPEC §6, hash ב-settings, 10 דק׳) · 8 השורות עם drill-down לכל מספר · חריגים חוסמים (§3.3 שלב 2) · "לשאול את ניסים" כאזהרה לא-חוסמת · **סגור חודש** → `periods.closed` + snapshot + משימה "להעביר X עד ה-10" + PDF · קישור ההעברה בפועל · יתרת פתיחה ("מנקים שולחן") · היסטוריה |
| 2 | **רווח והפסד** | מתג תפעולי/לחלוקה עם כותרת מפורשת (§3.2) · 4 KPI · הכנסות לפי תיק, הוצאות לפי קטגוריה עם drill לתנועות · השוואה בין ההגדרות · רווח לפי חודש · PDF |

**PDF (SPEC §7, §8):** Playwright HTML→PDF. אותו HTML של `/nissim/[month]/print` ו-`/pnl/print` משמש
להדפסה ול-PDF (הנחיה 20). נשמר ב-`storage/reports/` ונרשם ב-`report_runs`; שלב 9 מעלה ל-Drive.

**סגירת חודש — נבדק מקצה לקצה** (`scripts/e2e-close-month.mjs`, דרך ה-UI האמיתי): עם חריגים
חוסמים הכפתור מושבת והשרת מסרב; בלי חריגים — התקופה נסגרת עם snapshot, נוצרת משימת העברה
כשהחברה חייבת, מופק PDF, ו-INSERT לחודש הסגור נדחה ב-DB (§11.8).

**תיקון בדרך:** פרמטר jsonb שהועבר כ-`JSON.stringify(...)::jsonb` נשמר כמחרוזת JSON, ומפתח חלוקה
משותף היה נופל בשקט לברירת המחדל. תוקן בכל הקריאות, ונוסף אילוץ `007_jsonb_shape.sql` שדוחה
זאת ב-DB (הבטחה מבנית #25).

**למה ספטמבר לא נסגר:** (1) 7 תיקים "על בסיס הצלחה" עם נסח (18 ₪) ובלי חודש ובלי תקבול —
חריג חוסם אמיתי לפי §3.3; דן צריך לתת להם חודש או לסמן שההוצאה לא ישירה. (2) הכרעה #23
על 17,460 התקבולים. שניהם מוצגים במסך.

## שלב 4 — מה נבנה

| # | מסך | מה יש |
|---|---|---|
| 11 | **ייבוא (אשראי בלבד)** | העלאת CSV/XLSX בגרירה · זיהוי פורמט לפי כותרות (ישראכרט / מקס / כאל, מיפוי ניתן לעריכה ב-`settings.import_column_maps`) · "זוהה: ישראכרט · N שורות · טווח · יום חיוב" · **טבלת אישור** עם ההצעה מ-`rules`, "אוטומטי ✓" / "לבדוק ?" / "ידני", עריכה בשורה (סוג: הוצאה / מקדמה-ניסים / פרטי · פעילות · קטגוריה · עסקי/רכב · חשבונית · לשאול את…) · "אשר את כל האוטומטיים (N)" · **החל ייבוא** → תנועת-אב אחת (החיוב בבנק, `transfer`, `date_cash`=יום החיוב) + בנות עם `parent_id` בטרנזקציה אחת · שורה בלי קטגוריה → `unknown_expense` + משימה עם `auto_key` ייחודי · סיכום "N יובאו · X דולגו · Y לבדיקה → משימות · סכום בנות = אב ✓" · רשימת אצוות · רשימת כללים עם `hit_count` |
| 5 | **תנועות** | אחרי בחירת קטגוריה ידנית מופיע "להפוך לכלל?" (SPEC §4.4) |

**מנוע הכללים (§4.4)** — `lib/rules/classify.ts`, טהור: `classify`, `classifyBatch` (עם מדד האוטומציה),
`proposeRule` (התבנית = שם בית העסק בלי מספרי סניף/תאריך), `findConflicts`. הכתיבה — `createRuleFrom`:
אותה תבנית + אותו חשבון מעדכנת כלל קיים ולא יוצרת כפילות.

**ה-parser (§4.1)** — `lib/import/credit-card.ts`, טהור: `findHeaderRow` מזהה את שורת הכותרות גם מתחת
לכותרת-על, `parseCardStatement` מחזיר אב + בנות + אזהרות. dedup בתוך הקובץ לפי `card:<פורמט>:<מס׳ עסקה|בית עסק>:<תאריך>:<סכום>`,
ומול המערכת לפי `transactions.source_ref` (§11.9). זיכוי נשאר חיובי. CSV נקרא כ-UTF-8 (`lib/import/rows.ts`).

**ידמפוטנטיות:** `import_batches` ייחודי לפי hash הקובץ — אותו קובץ שוב מפנה לאצווה הקיימת (0 שורות חדשות);
שורה שכבר קיימת מייבוא קודם מסומנת "כבר קיים" ולא נכתבת. ביטול אצווה = soft delete, כדי שהקובץ יוכל לעלות שוב.

**נבדק מקצה לקצה** (`scripts/e2e-import.mjs`, דרך ה-UI): העלאה → זיהוי → סיווג ידני → "הפוך לכלל" → החל →
האב (−261.40) = סכום הבנות; WOLT ללא קטגוריה → `unknown_expense` + משימה; אותו קובץ שוב → האצווה הקיימת;
קובץ מקס → זיהוי "מקס". 22 בדיקות יחידה (`tests/import/credit-card.test.ts`, `tests/rules/classify.test.ts`) +
הבטחה מבנית 008 ב-`verify-db.sh`.

**⚠ מה לא אומת:** מיפויי העמודות של שלוש החברות נכתבו לפי הייצוא הנפוץ שלהן, **לא מול קובץ אמיתי** (שאלה #1).
הקבצים ב-`tests/fixtures/cards/` סינתטיים (מספרים מומצאים לבדיקה בלבד — לא נכנסים לשום seed). הקריטריון
"80%+ אחרי חודש" יימדד ב-KPI "סיווג אוטומטי" במסך רק אחרי חודש של קבצים אמיתיים.

## שלב 5 — מה נבנה

| # | מסך | מה יש |
|---|---|---|
| 21 | **מצב החברה** (דף הבית, ב.8) | 6 קוביות עם מספר גדול ו-drill: מזומן (עוגן + "עודכן לפני" + מסגרת + נקודה נמוכה 30/90) · הוצאות קרובות (7 ימים / עד סוף חודש / 3 הגדולות / "לא ירד כמצופה") · התחשבנות (יתרת ניסים MTD, מקדמות, ימים לסגירה; נדל"ן — שלב 7) · גביה וביצוע (ודאי/פוטנציאל + 3 התיקים ומה חסר) · רווח החודש (לחלוקה/תפעולי/יעד/3 חודשים) · דורש טיפול (7 מונים עם קישור) · פס KPI בסגנון WISE · 2 גרפים: נגבה לפי חודש (עמודות בפועל + קו צפוי) ופתוח לגביה (ודאי/פוטנציאלי) · פס סיכון |
| 3 | **תזרים 13 שבועות** (§3.6) | 4 מספרי הבוקר · עוגן inline · גרף קו כפול (ודאי / +משוקלל) עם קו אפס, קו מסגרת ונקודה נמוכה · טבלת שבועות עם drill לשורות · יומן 30 יום (ימים דחוסים >50K) · טאב **סטיות** = תור סגירת היום: "צפי / בפועל / סטייה / לא מוסבר", מה נרשם בין העוגנים, 3 כפתורים: סווג (פותח תנועה מוכנה וסוגר) / הוצאה קבועה חדשה / התעלם |
| — | **עוגן** `/anchor` | נייד-first: שדה אחד גדול, מקלדת מספרית, שמור = 3 טאפים (UIUX §6). אחרי שמירה: תוצאת סגירת היום + מספר ההתראות החדשות. 14 ימים אחורה |
| — | **➕ הזנה מהירה** `/quick` | 4 הזרימות של UIUX §6 ככפתורים גדולים |
| — | **התראות** `/alerts` (ב.11) | הפעמון מוביל לכאן. רשימה לפי חומרה, השהיה 3 ימים, "טופל", הצגת סגורות, "הרץ הערכה עכשיו", ריצה אחרונה, **יעדי מסירה** (וואטסאפ/מייל של דן) |

**סגירת יום (§3.6)** — `closeDay` ב-`lib/rules/cashflow.ts`: `predicted = עוגן קודם + Σ ודאי בין העוגנים`, `variance = בפועל − predicted` (ההתראה),
`unexplained = בפועל − (עוגן קודם + Σ תנועות שנרשמו)` (התור "מה קרה?"). נשמר ב-`daily_closes` (009). 4 בדיקות יחידה.

**ג'ובים (חלק ג')** — `day_close`, `alerts_eval`, `anchor_reminder` ב-`lib/jobs/`, כל ריצה ב-`scheduled_jobs_log`. מופעלים ב-`POST /api/jobs/<name>`
(מוגן ב-`JOBS_SECRET`) — `node scripts/jobs.mjs <name>` מה-cron, ו-`alerts_eval`+`day_close` רצים גם מיד אחרי הזנת עוגן ("אחרי כל ייבוא").
אין ג'וב שלא רשום בחלק ג' (הנחיה 14).

**מנוע ההתראות (ב.11)** — `alerts_eval` אוסף מה-DB את הקלט ל-13 מ-17 הכללים (הקיימים בנתונים: יתרה שלילית, חריגה 30 יום, סטיית יום, עוגן ישן,
הוצאה קבועה לא ירדה/חריגה, מע"מ ללא חשבונית, הכנסה ללא חשבונית, חשבונית לא שולמה, יתרת ניסים, מקדמות > חלק, תיק נרקב, ג'וב נכשל, תקציב),
ממזג לפי `rule_key` (הנחיה 18: קיימת → לא נוצרת שוב; חדלה → `resolved_at`), וכותב ל-`outbox` (הנחיה 16). לא מחוברים עדיין: חיוב אשראי > מסגרת
(אין מסגרת לכרטיס), משיכות נדל"ן (שלב 7), אינטגרציה מנותקת (ב.1).

**Green-API** — `lib/outbox/green-api.ts` (תחבורה בלבד, 4 בדיקות עם fetch מדומה) + `lib/jobs/outbox.ts` (תור, retry עם backoff 1/5/15/60/240 דק׳,
`max_attempts`, `dedup_key`). מפתחות בסביבת השרת בלבד (`GREEN_API_ID_INSTANCE`, `GREEN_API_TOKEN`). בלי מפתחות ההודעה נשארת `pending`
ומדווחת `noTransport`. מייל — עם שכבת Google (ב.1), אחרי שלב 5.

**נבדק מקצה לקצה** (`scripts/e2e-anchor.mjs`, 390px): עוגן ב-3 טאפים → סגירת יום (סטייה −10,000 מול עוגן אתמול) → `alerts_eval` → 6 התראות
(סטיית יום, מע"מ ללא חשבונית, 4 הכנסות ללא חשבונית) → מוצגות ב-`/alerts` → 13 שבועות בתזרים → 6 הקוביות בדף הבית. `anchor_reminder` הכניס
הודעה ל-`outbox` (waiting, אין תחבורה). **291 בדיקות יחידה.**

**⚠ הנחות שדורשות אישור:** #27 מע"מ לתשלום ב-15 לחודש על חבות החודש הקודם; מקדמה צפויה = ממוצע 3 חודשים ביום 19 (§3.6 "18–20").

## הנספח (אחרי שלב 5, הנחיה 19) — ב.1 ו-ב.4

| סעיף | מה נבנה | מצב |
|---|---|---|
| **ב.1 Google** | OAuth של המשתמש (`/api/google/connect` → Google → `/api/google/callback`), scopes מינימליים לפי שירות (Gmail readonly/send/modify, calendar.events, drive.file, spreadsheets) עם בחירה במסך, refresh token **מוצפן** (AES-256-GCM, `SECRETS_KEY` מחוץ ל-DB) ב-`integration_secrets` (010) — `integrations` מחזיקה מצב בלבד. חידוש אוטומטי; `invalid_grant` → `expired` → התראה "גוגל מנותק" + משימה דחופה "לחבר מחדש" (rule_key). לקוחות Gmail (שליחה, סריקה, תיוג) ו-Drive (עץ התיקיות של ב.1, העלאה ללא כפילות). מסך 18 `/settings`: כרטיס חיבור, ערוצים ומפתחות, בריאות המערכת 7 ימים, דוחות שהופקו | **◐ הקוד בנוי ונבדק עם fetch מדומה (14 בדיקות) + מסלול הכשל נבדק על ה-DB; החיבור האמיתי דורש OAuth client מ-Google Cloud של דן** |
| **ב.4 סיכום יום** | `lib/reports/daily-summary.ts`: 7 הסעיפים בסדר הקבוע, כל שורה עם קישור, "N פריטים · X דקות", מייל 3 שורות כשאין כלום, לא בשבת. **פונקציה אחת** למסך (`/reports/daily/[date]`), ל-PDF (Playwright על אותו HTML) ולמייל (הנחיה 20). ג'וב `daily_summary` (07:30): PDF → דרייב `סיכום-יומי/` (אם מחובר) → `report_runs` → מייל לדן דרך `outbox` עם ה-PDF מצורף (dedup ליום). "הפק סיכום יומי עכשיו" מהמסך | **✔ רץ על ה-DB המקומי: PDF הופק, `report_runs` נרשם, המייל ממתין ב-outbox עד שיש Gmail מחובר** |

**outbox — מייל:** `flushOutbox` שולח `email` דרך Gmail של דן כשיש `gmail.send`; אחרת ממתין ומדווח `noTransport: email`. תיקון בדרך: ה-outbox נכתב אחרי ה-commit של ההתראה (FK).

| סעיף | מה נבנה | מצב |
|---|---|---|
| **ב.3 קליטת חשבוניות** | **צינור אחד** לשלושה מקורות (`lib/intake/pipeline.ts`): Gmail (`gmail_scan`, שאילתת ב.3 + תווית "כספים/לקליטה", תיוג "נקלט"/"נכשל"), תיקיית Drive "00_להזנה" (`drive_intake_scan`), והעלאה ידנית מהנייד (`/import/inbox`, מצלמה). שלבים: שמירת הקובץ → סינון (חשבונית / דף פירוט → תור הייבוא / לא ידוע) → PDF→טקסט (pdf-parse) → חילוץ: **תבנית ספק** (נלמדת מהמסמך הראשון שאושר) → **כללים** (עברית/אנגלית, כולל RTL הפוך של pdf-parse) → **LLM** (Claude, structured output, רק לספק לא מוכר/ביטחון נמוך, `ANTHROPIC_API_KEY`) → `inbox_candidates` pending עם `verified=false` → משימה "חשבונית לאישור" (rule_key `inbox:<id>`). **"נכון ✓" = האישור האנושי** (הנחיה 17, נאכף ב-DB): `invoices(received)` → **שידוך** (`lib/match/invoices.ts`, §4.2: ±1 ₪, ±30 יום, ספק מנורמל; מלאה/מועמדים/ללא) → `transactions.invoice_status=has_invoice` → תבנית לספק → **העברה לחשבונית ירוקה** דרך outbox (אם `settings.greeninvoice_intake_email`; לא אם המסמך כבר בייצוא שלה) → המשימה נסגרת. חשבונית ללא תנועה → **תזרים** כ-committed בתאריך היעד. דרייב `חשבוניות/received/YYYY/MM/{תאריך}_{ספק}_{סכום}.pdf` כשמחובר | **✔ נבדק מקצה לקצה** (`scripts/e2e-intake.mjs`, 390px): PDF סינתטי → 100% חילוץ → אישור → חשבונית + שידוך + תבנית + משימה נסגרה → כפילות נחסמת. סריקת Gmail/Drive רצה רק אחרי חיבור גוגל |

| **ב.2 יומן** | `lib/rules/calendar-events.ts` (טהור, 10 בדיקות): כל 12 סוגי האירועים מהטבלה של ב.2 ל-60 יום — סגירת חודש (30, דן+ניסים, עם החריגים החיים), העברה (10, הסכום מהסגירה), מקדמה (18), אישור שכר (27, מי בטיוטה), תשלום שכר, סגירה לרו"ח (12, דן+הדס, צ'קליסט ✓/✗), מע"מ (15 / דו-חודשי), חיוב כרטיס לפי `billing_day`, דוח שבועי (ו'), תקבול committed (דן+הדס, "מה חסר"), תיק נרקב (לאחראי), עוגן. כל אירוע `[כספים]` + `rule_key` ב-`extendedProperties.private` → upsert ולא כפילות; fingerprint מונע עדכון סתם. **דרך outbox** (ערוץ `calendar`, הנחיה 16) → `lib/google/calendar.ts`. ג'וב `calendar_sync` (07:00). הגדרות במסך 18: יומנים (ראשי של הר-אל + אישי של דן), מיילים של ניסים/הדס, ימי מפתח. קריאת פגישות לשכר: `isEmployeeMeeting` + `listEvents` — החיבור למסך 19 בשלב 8b | **◐ רץ מקומית: 25 אירועים תוכננו ונכנסו ל-outbox, ריצה שנייה = 0 חדשים; הכתיבה ליומן מחכה לחיבור גוגל** |

| **ב.5+ב.6 גביה וביצוע** | **מסך 20 `/collections`** בשלושה טאבים: **גביה** — 4 קוביות גיול (0–30 / 31–60 / 61–90 / 90+, הצבע מתחמם, drill לכל קובייה) וטבלה עם לקוח, פתוח מפוצל ודאי/פוטנציאלי, תאריך יעד, איחור, **מסמך** (לא הוצא → חשבונית מס הוצאה → שולם → קבלה), סטטוס גביה, מה חסר, פעולה אחרונה, וכפתורים בשורה: **שלח תזכורת** (preview לפני שליחה, UIUX §5.7, הטון מתחמם לפי הגיול, דרך outbox) · **סמן שולם** (תקבול חדש או שיוך תנועה קיימת; סטטוס הגביה מתעדכן לבד) · **הוצא חשבונית** (deep-link לחשבונית ירוקה — **המערכת לא מנפיקה מסמכי מס**) · **טיפול משפטי** (מ-90 יום, יוצר משימה). **לביצוע וגביה** (ב.5) — הפילוח "מה מעכב את הכסף ברמת החברה" + טבלת תיקים עם הפריט הבא שחסר, האחראי, כמה ימים תקוע ואחוז השלמה. **דגלים** (ב.6) — שלושת הדגלים האוטומטיים כ-view. `collection_actions` מתעד כל תזכורת/סימון/העברה | **✔ רץ על הנתונים המוסווים: 19 תיקים, 727,253 ₪ פתוחים; 660,253 ₪ תקועים ש-20 מהם מחכים ל"מקדמה התקבלה"** |

**ב.5 — הצ'קליסט נולד בייבוא:** `scripts/import-workbook.mjs` מייצר לכל תיק מיובא צ'קליסט מהתבנית לפי מוצר, ומסמן אוטומטית מה שכבר קרה (הסכם חתום אם התיק לא "פוטנציאל", שכ"ט נגבה אם נכנס כסף). בלעדיו "מה חסר כדי לקבל את הכסף" היה ריק. **פריט תקוע 7 ימים → משימה לאחראי** (ב.10) ב-`alerts_eval`.

**מה חסר ב-ב.3:** OCR עברי לסרוקים (#31 — עד אז: LLM על ה-PDF עצמו אם יש מפתח, אחרת "נדרש OCR" והזנה ידנית) · תיקיית "להזנה" דורשת `drive.readonly` (#30) · ייצוא חשבונית ירוקה לזיהוי כפילויות (שלב 6).

**⚠ סטייה לאישור (#28):** ADDENDUM ב.1 אומר "Supabase Vault". בשלב הנוכחי (Postgres רגיל) המקבילה היא הצפנה עם מפתח שיושב רק בשרת. המעבר ל-Vault = החלפת `seal/open` ב-`lib/secrets.ts`.

| **ב.7 דוח שבועי + P&L אוטומטי** | **דוח שבועי** (`lib/reports/weekly.ts`, ג'וב ו' 08:00): תשע השורות של ב.7 בעמוד אחד — השבוע במספרים · MTD מול יעד ומול אותו שלב בחודש הקודם · P&L MTD בשתי ההגדרות · התחשבנות ניסים כמצב ביניים · גביה עם 5 הגדולים ומה זז השבוע · פייפליין והמרה לפי ערוץ · **13 שבועות קדימה כגרף עמודות** · חריגים · לשבוע הבא. **שלוש גרסאות לפי הרשאה** (החלטה 21): דן הכול · ניסים מימון **בלי יתרת בנק** (§6) · אביב נדל"ן. **P&L אוטומטי**: `pnl_draft` (1 לחודש, טיוטה) ו-`pnl_final` (12 לחודש) — מאותו מנוע של מסך 2 (הנחיה 20); הסופי מופק **רק אם הצ'קליסט מלא**, אחרת נוצרת משימה. **`accountant_pack`** (12 לחודש): מייל אחד לרו"ח עם כל הדוחות והצ'קליסט. **צ'קליסט סגירת חודש** — 8 הפריטים של ב.7, כל ✓ אוטומטי מהנתונים, מוצג במסך 2 ובאירוע היומן של ב.2 | **✔ רץ: הדוח השבועי הופק ב-3 גרסאות (PDF + outbox); `pnl_final` סירב נכון עם 3 חסמים** |

**עוד שני ג'ובים מחלק ג':** `deal_decay` (02:00 — §2.3 רקב, מעדכן הסתברות ומשימה לאחראי) · `db_backup` (03:00 — תמצית JSON דחוסה של 24 טבלאות לדרייב `גיבוי-DB/`; **לא תחליף לגיבוי של Supabase**).

## שלב 6 — מה נבנה

| נושא | מה נבנה | מצב |
|---|---|---|
| **§4.2 דף בנק** | `lib/import/bank.ts` — מיפויי עמודות להפועלים / לאומי / דיסקונט / מזרחי + גנרי, זיהוי אוטומטי לפי הכותרות, חובה/זכות או עמודת סכום אחת, תאריך ערך מול תאריך פעולה. **אימות היתרה המתגלגלת**: פתיחה + התנועות = סגירה, ואם לא — מוצגת **השבירה הראשונה** (איפה בדיוק חסרה שורה בקובץ) ולא רק "יש פער" | **✔ 7 בדיקות יחידה** |
| **§4.2 שידוך** | כל שורת בנק מחפשת תנועה קיימת באותו חשבון (±1 ₪, ±3 ימים, `lib/match/invoices.ts`) — מלאה / מועמדים / ללא. מסך אישור נפרד לבנק (`app/import/[id]/bank-review.tsx`): מה שודך, מה להכריע (בורר מועמדים בשורה), מה ייכנס כתנועה חדשה. שורה ששודכה **לא נוצרת מחדש** — היא מסומנת "אומת מול דף הבנק" | **✔ e2e: 3 שודכו, 3 נוצרו** |
| **§4.3 חשבונית ירוקה** | `lib/import/greeninvoice.ts` — 14 העמודות, הסכומים תמיד מ"סכום הוצאה עסקית" בשקלים (מט"ח מומר אצלם), חשבונית זיכוי שלילית, **dedup** לפי (מס' מסמך + ספק מנורמל + סכום) בתוך הקובץ ומול המערכת, `gi_account_key` לפלט לרו"ח. `lib/import/greeninvoice-load.ts` — קליטה idempotent (hash + `source_ref`), שידוך לתנועות, ו**חשבונית של ישות שותף** (די.אנד.די / אביב / ניסים) מסומנת `needs_partner_review` + משימה — משיכה ולא הוצאה (שאלה #15) | **✔ 8 בדיקות יחידה + e2e** |
| **מסך 10 — מע"מ** | `/vat`: חבות לפי תקופת דיווח (חודשית או דו-חודשית לפי `settings.vat_bimonthly`), כל מספר נפתח לשורות (`/api/vat/lines`), "תשומות ללא חשבונית — 18 חודשים" כסכום נפרד, והתשלום הבא שנכנס אוטומטית לתזרים | **✔** |
| **מסך 12 — פערים** | `/gaps`: שלוש הבדיקות של §4.3 על 12 חודשים, drill לכל תא, סימון "לא נדרשת חשבונית" בשורה (עמלות בנק, מס), הכרעת חשבוניות שותפים, ו**"שלח לרו״ח"** — הפקה ← PDF ← outbox ← `report_runs` (שמסמן ✓ בצ'קליסט סגירת החודש של ב.7) | **✔ קריטריון שלב 6 רץ** |
| **ג'וב 14 מחלק ג'** | `greeninvoice_import` (א' 09:00) — אין API לחשבונית ירוקה (#17), ולכן הג'וב בודק שהייצוא השבועי נקלט, ואם לא — פותח משימה עם `rule_key` ייחודי ומציין כמה מע"מ בסיכון. ברגע שיהיה API, אותו ג'וב יקרא אותו | **✔** |

**תיקון ב-v_vat (§3.1):** `sum(...) filter (...)` החזיר `NULL` בחודש בלי תשומות מוכרות, והחבות "נעלמה" במסך (0 במקום 22,260). עכשיו `coalesce(...,0)` — כמו ב-`lib/rules/vat.ts` — ובדיקה מבנית חדשה מוודאת שאין `NULL` בחבות.

**מה שלב 6 לא כולל:** WISE (§4.5, שלב 8) · מיפויי העמודות של הבנקים אומתו על קבצים סינתטיים בלבד — קובץ אמיתי אחד מכל בנק יאשר או יתקן אותם (שאלה #1).

## שלב 7 — מה נבנה

| נושא | מה נבנה | מצב |
|---|---|---|
| **מסך 8 — משיכות שותפים** 🔒 | `/partners`: רווח לחלוקה נדל"ן לפי שנה (§3.5 — **בלי** דרישת אישור ו**בלי** דרישת `deal_id`, בשונה מ-§3.2), יעד שוויוני = רווח ÷ מספר השותפים, מה נמשך בפועל, והסטייה עם תגית "משך יותר / משך פחות / מאוזן". רישום משיכה מהמסך (שכר · דמי ניהול · דיבידנד · הלוואת בעלים · החזר), עם סימון "כולל עלות מעביד" ליוני. **חו"ז בעלים בנפרד** — הלוואה והחזר אינם משיכה מול היעד | **✔** |
| **"החלק שלי"** | `/partners/[id]` — מה שאביב או יוני רואים: היעד שלו, מה משך, כמה נשאר, פס התקדמות, החו"ז שלו והמשיכות שלו. **המספרים של האחרים לא מוצגים שם** | **✔ קריטריון שלב 7** |
| **חוב משקיע** | §3.5 "200,000 פתיחה פחות החזרים, מוצג עם קצב ירידה": יתרת הפתיחה יושבת על השותף (`partners.investor_loan_opening`), **ברירת המחדל 0** — דן מזין אותה במסך. המערכת לא כותבת לעצמה מספר שלא אושר (שאלה #8). כשיש החזרים מוצג הקצב החודשי וכמה חודשים לסגירה | **✔** |
| **נכיון מזומן** | §3.5 "מוצג בנפרד בדוח (לא מוסתר)" — `v_cash_discount` וטבלה משלו במסך | **✔** |
| **מסך 9 — הכנסות פרייבט** 🔒 | `/private`: טבלת קרנות עם סכום העסקה, אחוז, סכום, חלוקה דן/ניסים לכל שורה, צפוי מול התקבל, סימון "התקבל" (תאריך + לאיזה חשבון פרטי), שינוי חלוקה, והוספת שורה. `lib/rules/private.ts` מחלק ב-`allocateMoney` כדי שלא תיעלם אגורה | **✔ 8 בדיקות יחידה · רץ על 8 שורות אמיתיות מהקובץ** |
| **חוקי סף** | §2.1 נותן דוגמה אחת ("מעל 3M חודשי → 1%"). `evaluateThreshold` מכיר שתי צורות מפורשות ומחזיר `null` עם סיבה לכל צורה אחרת — **המערכת לא מנחשת אחוז** (שאלה #7) | **◐ ממתין לחוקים האמיתיים** |
| **§6 — קוד לכל אזור** | תוקן: `<PinGate>` שמר את הפתיחה תחת מפתח אחד לכל האזורים, כך שפתיחת כרטיס ניסים פתחה גם את פרייבט. עכשיו מפתח לכל אזור — פתיחה אחת לא פותחת את השאר | **✔** |

**מה שלב 7 לא כולל:** הרשאות אמיתיות לאביב וליוני (משתמשים, תפקידים, RLS — שלב 10). היום ההגנה היא ה-PIN של §6, ו"החלק שלי" הוא המסך שיינתן להם ברגע שיהיו משתמשים.

**אין עדיין נתוני נדל"ן:** בקובץ האמיתי אין תנועות `realestate` ואין משיכות שותפים — 28 התיקים כולם מימון. המסך נבדק מקצה לקצה על פעילות זמנית שהבדיקה מייצרת ומבטלת (`scripts/e2e-partners.mjs`), ויתמלא ברגע שתיכנס עסקת נדל"ן ראשונה.

## שלב 8 — מה נבנה

| נושא | מה נבנה | מצב |
|---|---|---|
| **§4.5 ייבוא WISE** | `lib/import/wise.ts` — זיהוי אוטומטי של שלושת הייצואים (מתעניינים / לקוחות / הגשות) לפי הכותרות · מיפוי שמונת סטטוסי WISE לרמות המשפך · **זיהוי ערוץ**: `car-05XXXXXXXX@car.com` → רכב-לנדינג + שעבוד רכב · נרמול טלפון (972, מקפים, אפס חסר) · dedup שבו השלב המתקדם גובר ו"אבוד" לא דורס התקדמות | **✔ 9 בדיקות יחידה** |
| **idempotency ושמירה על מה שלנו** | `lib/import/wise-load.ts` — `wise_ref` לכל ישות, ריצה חוזרת מעדכנת ולא מכפילה, ו**שכ"ט, חודש התיק ולוח התשלומים לא נדרסים** מ-WISE (§4.5). שורה שבוטלה ומיובאת שוב מוחייה במקום להתנגש באינדקס הייחודי | **✔** |
| **הגשות מזינות את ההסתברות** | הגשה → התיק מתקדם ל"הוגש", אישור → "מאושר עקרונית" (§2.3). תיק שכבר התקדם ידנית לא נסוג | **✔ נבדק ב-e2e** |
| **מסך 14 — לידים והמרה** | `/leads`: משפך **ב-₪** (עלות לידים בצד אחד, שכ"ט שנחתם ונגבה בצד השני) · טבלה שבועית מ-`v_conversion` (המרה, CAC, ROI) · היטמאפ ערוץ × מוצר · **מסקנות אוטומטיות** (§3.8: CAC מעל ממוצע ×1.5, ערוץ בלי חתימה 4 שבועות) · השלמת עלויות עם **פריסה לשבועות** (חבילת 2.5 חודשים לא מקפיצה את ה-CAC בשבוע אחד) · תיקון ערוץ בשורה | **✔** |
| **מסך 13 — תחזית רבעונית** | `/forecast`: שלושה טאבים (מאוחד / נדל"ן / מימון), **שני תרחישים נבחרים בנפרד** (§3.7.3 — נדל"ן פסימי + מימון בסיס זה שילוב חוקי), הנחות שאפשר לדרוס — והדריסה נשמרת בכתובת וניתנת לשיתוף — ודיוק תחזית היסטורי מ-`forecast_snapshots` | **✔** |
| **ג'וב 15 מחלק ג'** | `wise_import` (א' 09:30) — אין API ל-WISE (#14), ולכן הג'וב בודק שהייצוא השבועי נקלט, ואם לא פותח משימה עם `rule_key` ייחודי ומציין כמה לידים תקועים | **✔** |

**מה שלב 8 עוד לא הוכיח:** הקריטריון של §9 הוא "277 המתעניינים ו-35 ההגשות מ-WISE במערכת". הפורמט נבנה לפי מה שהאפיון מתעד, ונבדק על קבצים סינתטיים — **הייצואים האמיתיים עוד לא נראו**. כשהם יגיעו, ייתכן שיהיו הבדלי כותרות, והתיקון הוא במיפוי בלבד.

**עוד תיקון בדרך:** `scripts/server-install.sh` הריץ את כל קובצי הסכימה בכל התקנה, וקובץ `alter table add column` נכשל בהרצה שנייה. עכשיו יש `schema_migrations` — כל קובץ סכימה רץ פעם אחת ונרשם, ה-views וה-seed מתרעננים תמיד.

## העלאה לאוויר — מה מוכן

| | מצב |
|---|---|
| **`docs/DEPLOY.md`** | מסלול מלא: Supabase → משתני סביבה → Vercel → נתונים ראשונים → cron → חיבור גוגל → אבטחה → גיבוי |
| **`vercel.json`** | שבעת הג'ובים של חלק ג' כ-Vercel Cron (UTC+3), תקרות זמן ל-PDF ולג'ובים |
| **`.github/workflows/cron.yml`** | אותם ג'ובים כ-GitHub Actions — עובד גם בתוכנית החינמית (Vercel Hobby מגביל ל-cron יומי) |
| **`/api/health`** | 200 כשה-DB עונה והסכימה במקום; מונה ג'ובים שנכשלו ו-outbox תקוע. לא חושף נתונים עסקיים |
| **`node scripts/preflight.mjs`** | אומר מה **חוסם** עלייה לאוויר, מה רק **מגביל**, ומה כבר עובד — עם הפקודה לתיקון בכל שורה |
| **אימות ג'ובים** | `x-jobs-secret` (ה-cron שלנו) או `Authorization: Bearer $CRON_SECRET` (הפורמט של Vercel); GET ו-POST |
| **PDF בפרודקשן** | `lib/pdf.ts` נופל אוטומטית ל-`@sparticuz/chromium-min` ב-serverless; צריך רק `CHROMIUM_PACK_URL` |
| **שער גישה זמני** | `APP_PASSWORD` + `middleware.ts` + `/gate` — קוד אחד לכל האתר, כניסה גם דרך `?key=`. `/api/health` ו-`/api/jobs` נשארים פתוחים ל-cron. **זה לא §6** (אין משתמשים, תפקידים או תיעוד כניסות) — מנעולת דלת עד שלב 10 |

**מה חוסם היום:** אף אחד מהחסמים אינו קוד. צריך: פרויקט Supabase, פרויקט Vercel, ו-OAuth client של גוגל — הכול ב-`docs/DEPLOY.md`.

## מה **לא** נבנה ולמה

- **4 ג'ובים מחלק ג'** עדיין לא רצים: `payroll_reminder` ו-`accountant_payroll_send` (מסך 19, שלב 8b),
  `reports_to_drive` ו-`cold_backup` (שכבות הגיבוי של §6, שלב 9). **15 רצים.**
- **כל ה-parsers קיימים** (§4.1 אשראי, §4.2 בנק, §4.3 חשבונית ירוקה, §4.5 WISE), אבל **אף אחד מהם
  לא נבדק על קובץ אמיתי** — המיפויים נכתבו לפי האפיון (שאלות #1, #2).
- **RLS והרשאות (§6).** הטבלאות והתפקידים קיימים; המדיניות עצמה שייכת לשלב 10.
- **הקובץ האמיתי.** ראו את ההערה בסוף `docs/OPEN_QUESTIONS.md` — זו המגבלה
  המשמעותית ביותר במצב הנוכחי.

## הצעד הבא

1. **לעלות לאוויר:** `node scripts/preflight.mjs` ואז `docs/DEPLOY.md`. אין חסם קוד.
2. **להתחיל להזין עוגן יומי** (`/anchor` מהנייד) ולהגדיר יעד וואטסאפ ב-`/alerts` + מפתחות Green-API בשרת — זה קריטריון שלב 5 בפועל.
3. **קובץ אשראי אמיתי אחד** מכל חברה שבשימוש (שאלה #1) — לאמת את מיפויי העמודות ולהתחיל את חודש המדידה של שלב 4.
4. לסגור את ספטמבר: חודש ל-7 התיקים החוסמים + הכרעה #23 (שלב 3).
5. הכרעות #25–#32.
6. **לחבר את גוגל:** OAuth client ב-Google Cloud Console (redirect `<APP_BASE_URL>/api/google/callback`), `GOOGLE_CLIENT_ID/SECRET` + `SECRETS_KEY` בשרת, ואז "חבר את גוגל" ב-`/settings`. עד אז המיילים ממתינים ב-outbox.
7. **דף בנק אחד אמיתי + ייצוא הוצאות אחד מחשבונית ירוקה** (שאלה #1) — לאמת את מיפויי העמודות של שלב 6 ולהפיק דוח פערים על נתוני אמת.
8. **מייל הרו"ח ב-`/settings`** (`accountant_email`) — בלעדיו הדוח מופק אבל לא יוצא לאף אחד.
9. **עסקת נדל"ן ראשונה במערכת** — בלעדיה מסך 8 מלא מבנה וריק מנתונים.
10. **יתרת הפתיחה של חוב יוני** (#8) ו**חוקי הסף של הקרנות** (#7) — שתיהן חוסמות תמונה מלאה בשלב 7.
11. **ייצוא אחד מכל מסך ב-WISE** (מתעניינים, לקוחות, הגשות) — זה מה שחסר לקריטריון של שלב 8.
12. הנספח הושלם (ב.1–ב.8, ב.11), ושלבים 6, 7 ו-8 נבנו. הבא: **שלב 8b** — שכר (מסך 19). ל-ב.3 מול מיילים אמיתיים: ANTHROPIC_API_KEY בשרת (LLM כהצעה) ו-`greeninvoice_intake_email` בהגדרות.

הנספח (ב.1–ב.11) נכנס אחרי שלב 5, לפי ADDENDUM הנחיה 19.
